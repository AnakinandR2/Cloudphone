# 后端 API 根地址（仅服务端用，SSR 调 /user/me 拉登录态）。默认 http://localhost:9981/api/v1。
# NUXT_BACKEND_BASE_URL=http://localhost:9981/api/v1

# 内容中台 Pub API（服务端私有）。复制为 .env 并填入真实值；切勿提交真实密钥。
#  - NUXT_PUB_BASE_URL    开放 API 根地址（含 /api/open/v1）
#  - NUXT_CONTENT_API_KEY 工作空间只读密钥（cp_ 开头）
#  - NUXT_CONTENT_SPACE   博客内容空间 slug（默认 blog）
NUXT_PUB_BASE_URL=http://192.168.10.110:9981/api/open/v1
NUXT_CONTENT_API_KEY=cp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# NUXT_CONTENT_SPACE=blog

# my（用户控制台）地址前缀：登录/注册/控制台跳转目标。
# 不设置时为同源相对路径 /my/（生产同源部署）；
# 跨域 / 开发态 my 跑在独立端口时设置完整地址，例如：
# NUXT_PUBLIC_MY_APP_PATH=http://127.0.0.1:5666/my

# 用户登录态 Cookie 名：须与后端 USER_JWT_COOKIE_NAME 一致（默认 user_token）。
# NUXT_PUBLIC_USER_COOKIE_NAME=user_token
