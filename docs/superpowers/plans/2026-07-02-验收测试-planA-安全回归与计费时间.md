# 上线验收 Plan A：安全回归矩阵 + 计费时间测法 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (推荐) 或 superpowers:executing-plans 逐任务实施。步骤用 checkbox（`- [ ]`）跟踪。

**Goal:** 把《[上线验收测试计划总纲](../specs/2026-07-02-上线验收测试计划-design.md)》里的"安全回归矩阵（严重/高）+ 计费时间测法"落成可自动化、可重复运行的测试套件，作为上线前 go/no-go 的核心回归门，并在过程中暴露并修复仍存的生产缺陷。

**Architecture:** 分两层落测——① 跨子系统与越权/资金类走 `backend/apptest`（真实 HTTP 路由 + 全局 `framework.DB` 原生 SQL），零浏览器、可重复；② 需传 `now`/直调 repository/service/喂纯函数的时间与事务类走各模块 `internal/*_test.go`（真实 sqlite）。绝大多数严重/高危缺陷已有单测，本计划**不重写**，只补 apptest HTTP 层缺失断言与仍未覆盖项；对测试暴露的未修缺陷（I2/S4）走"发现→修→记录"。

**Tech Stack:** Go + Gin + GORM + testify（require/assert）；测试库默认 sqlite（`framework.SetupTestDB`）；`framework.Response{Code,Message,Data}` 统一响应包络；`framework/apperr` + `FailErr` 错误映射（403/404/409/422）。

## Global Constraints

以下为**所有任务隐含遵守**的项目级约束（每条来自 backend/CLAUDE.md 或已核对的真实基建）：

- **`framework.DB` 是包级变量 `*gorm.DB`（不是函数，勿写 `framework.DB()`）**；apptest 与 internal 单测均可 `framework.DB.Exec/.Raw/.Where/.Table(...)`。
- **apptest（`package apptest`）只经公开 HTTP API + `framework.DB` 原生 SQL 造数/断言；绝不 import 任何模块 `internal`**（Modulith 边界，`framework/arch_test.go` 编译期强制）。表名取自各 model 的 `TableName()`：`staff`/`roles`/`role_permissions`/`staff_roles`/`users`/`cloud_phones`/`proxies`/`billing_biz_orders`/`billing_ledger_entries`/`billing_license_units`/`billing_trial_claims`/`access_logs`。
- 需要**传 `now`、直调 repository/service、喂纯函数**的测试放对应模块 `internal/*_test.go`（真实 sqlite，经该包已有的 `TestMain`→`framework.SetupTestDB`+各 `Module.Init` 装配；直接用 `LicenseService`/`FulfillService` 等包级实例）。
- **共享 helper 只定义一次**（同包重名即编译失败）：
  - 已存在、直接复用勿重定义：`setupRouter()`、`doJSON(r,method,path,token,body)`、`decode(t,w)`、`login(t,r,account,pwd)`、`adminToken(t,r)`、`createUser(t,r,adminTok,username,pwd,superuser)`（`apptest/helpers_test.go`）、`registerUser(t,r,phone)`（`apptest/note_test.go:18`，密码固定 `pass123`）。
  - 本计划新增包级 helper 各定义一次：`staffIDByUsername`/`staffIsSuperuser`/`createRole`/`assignRoles`（Task 2，`apptest/staff_rbac_test.go`），`grantStaffDelete`（Task 3，同文件），`cleanupLicenseUnitsByUser(t,uid int)`（Task 10，`billing/internal`，Task 11 复用）。
- **唯一命名 + `t.Cleanup` 清自己造的行；绝不 `CleanTable` 截断共享表**（`RunSetup` seed 了 admin/角色/示例只读角色）。
- `LicenseUnit.UserID` 是 `uint`，而 `activeUnits(userID int, ...)` 取 `int`——构造/调用时注意类型转换（`UserID: uint(uid)`）。
- **提交前必须**：`gofmt -w <改动文件>`（全 LF）、`go vet ./...` 干净、`go test ./... -count=1`（含 `./apptest/`）全绿、`go test ./framework/ -run TestModuleBoundaries` PASS（Modulith 边界未破）。
- **发现生产 bug（TDD 测试先失败）**：直接修生产码 + 在 Task 1 的审计文档逐条记录（依 `fix-and-record-bugs-during-testing` 约定）。**不得为迁就测试放宽安全守卫**——修的是缺陷，不是断言。
- 提交信息中文、语义化前缀（`test(...)`/`fix(...)`），**结尾不加任何署名行**。

## File Structure

| 文件 | 动作 | 职责 |
|---|---|---|
| `docs/测试/安全回归覆盖审计-2026-07-02.md` | 新建 | Task 1：finding→已有测试/本计划任务 映射 + 基线结果 + 已知未修项 |
| `backend/apptest/staff_rbac_test.go` | 新建 | Task 2/3：RBAC 越权铸造/提权、删除守卫、禁用失效（HTTP） |
| `backend/apptest/phone_security_test.go` | 新建 | Task 4/5：status 越权、代理绑定属主（HTTP） |
| `backend/modules/proxy/proxy.go` | 修改 | Task 5：新增 `GetByID` 门面（I2 修复） |
| `backend/modules/proxy/internal/{service.go,repository.go}` | 修改 | Task 5：属主查询方法（I2 修复） |
| `backend/modules/phone/internal/service.go` | 修改 | Task 5：Create/Update 校验 proxy 属主（I2 修复） |
| `backend/apptest/proxy_test.go` | 追加 | Task 6：探测 SSRF 拦截（HTTP，S2） |
| `backend/apptest/billing_pay_test.go` | 新建 | Task 7：重复/并发 mark-paid 幂等（B1/B2） |
| `backend/modules/billing/internal/bizorder_service_test.go` | 追加 | Task 8：履约失败整单回滚（B3） |
| `backend/apptest/billing_trial_test.go` | 新建 | Task 9：试用超领 + 唯一约束（B5） |
| `backend/modules/billing/internal/license_repository_test.go` | 新建 | Task 10：到期读时过滤 + markExpired（X9） |
| `backend/modules/billing/internal/fulfill_service_test.go` | 追加 | Task 11：续费叠加 + 永久单元 |
| `backend/apptest/billing_expiry_test.go` | 新建 | Task 12：到期链路 HTTP E2E（seed→backdate→容量降） |
| `backend/modules/accesslog/internal/api.go` | 修改 | Task 13：分页 size 钳制（S4 修复） |
| `backend/apptest/accesslog_test.go` | 追加 | Task 13：分页钳制回归 |

## 任务分组

- **基线**：Task 1
- **后台 RBAC 越权**：Task 2、3
- **前台越权（phone/proxy）**：Task 4、5、6
- **计费资金安全**：Task 7、8、9
- **计费时间/到期**：Task 10、11、12
- **框架安全缺口**：Task 13

> 说明：C1（中台失败列表，已有 `midplat_failedlist_test.go` 覆盖）、O6（`fullKey` 脱敏，可利用性存疑）、C6（差集回报，需 fake 中台基建）、B4/B6（并发/上限中危）**不在 Plan A**，归后续 Plan D；本计划不静默省略，明确记录于 Task 1 审计文档。

---

### Task 1: 回归基线绿 + 安全覆盖审计

**类型**：基线（无生产改动；产出审计文档）

**Files:**
- 运行：`backend/`（编译/vet/test/边界）
- Create: `docs/测试/安全回归覆盖审计-2026-07-02.md`

**Interfaces:**
- Consumes: 无
- Produces: 审计文档，供 Task 2–13 引用"哪些 finding 已有测试 / 本计划补哪条 / 哪些不在 Plan A"

- [ ] **Step 1: 跑回归基线并记录**

Run:
```
cd backend && go build ./... && go vet ./... && go test ./... -count=1
```
Expected: 编译通过、vet 干净、`go test` 全部 `ok`（若有既有 FAIL，如实记录到审计文档"基线现状"，不隐瞒）。

- [ ] **Step 2: 跑 Modulith 边界自检**

Run:
```
cd backend && go test ./framework/ -run TestModuleBoundaries -v
```
Expected: PASS（apptest 未 import 任何模块 internal）。

- [ ] **Step 3: 写安全覆盖审计文档**

新建 `docs/测试/安全回归覆盖审计-2026-07-02.md`，含：
1. 基线结果（Step 1/2 的真实输出摘要）。
2. 一张映射表（严重/高/入选中危 每条 finding → 已有测试文件 | 本计划 Task N | 不在 Plan A）：

```markdown
| 编号 | 等级 | 已有测试 | Plan A 补测 | 说明 |
|---|---|---|---|---|
| S1/A1/A2/A3 | 严重/高 | role_*/service 单测 | Task 2 | 补 apptest HTTP 越权断言 |
| A4/A5/X2 | 高/中 | user 单测 | Task 3 | 补删除守卫+禁用失效 HTTP |
| I1/X10 | 高 | phone api_test | Task 4 | 补 HTTP 层 status 忽略 |
| I2/X11 | 中(BOLA) | 无 | Task 5 | **仍未修**：补测暴露→修 |
| S2 | 高 | prober_ssrf_test | Task 6 | 补 HTTP 拦截断言 |
| B1/B2 | 严重/高 | bizorder_cas_test | Task 7 | 补 HTTP 幂等 |
| B3 | 高 | bizorder_service_test | Task 8 | 补履约失败回滚 |
| B5 | 中 | trial_service_test | Task 9 | 补超领+唯一约束 |
| X9 到期 | — | license_service_test | Task 10/11/12 | 计费时间测法 |
| S4 | 中 | 无 | Task 13 | **仍未修**：补测暴露→修 |
| F1/F2/O3/C1 | 严重/高 | 各有单测 | 引用 | 已覆盖，审计确认即可 |
| S3 | 中 | framework/middleware_test.go | 引用 | CORS 白名单已有单测，审计确认即可 |
| 席位溢出回收 | — | license_reconcile_test.go | 引用 | planSeatAssignment 纯逻辑已覆盖 |
| C6/O6/B4/B6 | 中/低 | 部分 | 不在 Plan A | 归 Plan D |
```

- [ ] **Step 4: 无需运行**（纯文档）。

- [ ] **Step 5: 提交**

```
git add docs/测试/安全回归覆盖审计-2026-07-02.md
git commit -m "test(acceptance): Plan A 安全覆盖审计与回归基线记录"
```


---

### Task 2: 后台 RBAC 越权铸造/提权回归（S1/A1/A2/A3）

**类型**：回归防护（S1/A1/A2/A3 的授权收敛层已在 `role_service.go`/`service.go` 落地，测试应立即通过）

**Files:**
- Create/Test: `apptest/staff_rbac_test.go`（新建）

**Interfaces:**
- Consumes:
  - `setupRouter() *gin.Engine`
  - `doJSON(r *gin.Engine, method, path, token string, body interface{}) *httptest.ResponseRecorder`
  - `decode(t *testing.T, w *httptest.ResponseRecorder) framework.Response`（`framework.Response{Code int, Message string, Data interface{}}`）
  - `adminToken(t *testing.T, r *gin.Engine) string`（内置超管 admin/admin123）
  - `createUser(t, r, adminTok, username, password string, superuser bool)`（经 `POST /api/v1/staff/create`）
  - `login(t, r, account, password string) string`
  - `framework.DB *gorm.DB`
- 真实路由/字段（已 Read 核对）：
  - `POST /api/v1/role/create`（权限 `role:create`），请求体 `RoleCreate{name(必填), description, permissions []string}`，成功 Data 为 `Role{id,...}`
  - `POST /api/v1/staff/create`（权限 `staff:create`），请求体 `StaffCreate`(=`StaffForm`) 字段 `username/password/is_active/is_superuser/name/avatar`，**无 role_ids**；成功 Data 为 `Staff{id, is_superuser,...}`
  - `PUT /api/v1/staff/update/:id`（权限 `staff:edit`），请求体 `StaffUpdate`，赋角色靠 `role_ids (*[]int)`，改超管位靠 `is_superuser (*bool)`
  - 表名：`staff`（列 `username`/`is_superuser`），`roles`，`role_permissions`（列 `role_id`/`permission`），`staff_roles`（列 `staff_id`/`role_id`）
  - 权限标识（与 `permissions.go` 一致）：`staff:create`、`staff:edit`、`role:create`、`role:view`、`billing:manage`
  - 业务错误映射：`apperr.Forbidden→403`
- Produces: 测试函数 `TestStaffRBAC_MintAndEscalationGuards(t *testing.T)`

- [ ] **Step 1: 写失败测试** —— 新建 `apptest/staff_rbac_test.go`：

```go
package apptest

import (
	"fmt"
	"net/http"
	"testing"
	"time"

	"manager-backend/framework"

	"github.com/gin-gonic/gin"
	"github.com/stretchr/testify/require"
)

// TestStaffRBAC_MintAndEscalationGuards 回归后台 RBAC 授权收敛层：
// S1 铸造越权角色、A2 创建时自封超管、A1 更新时提超管位、A3 赋越权角色，均应被挡。
func TestStaffRBAC_MintAndEscalationGuards(t *testing.T) {
	r := setupRouter()
	admin := adminToken(t, r)
	db := framework.DB

	// 唯一命名，避免污染共享表。
	suffix := fmt.Sprintf("%d", time.Now().UnixNano())
	limitedName := "rbac_limited_" + suffix   // 受限管理员
	victimName := "rbac_victim_" + suffix     // 被更新的普通员工
	roleName := "rbac_role_limited_" + suffix // 受限管理员自身角色
	privRoleName := "rbac_role_priv_" + suffix // 含 billing:manage 的高权角色

	// t.Cleanup 逆序清理本用例造的行（先删关联，再删主体）。
	var limitedID, victimID, roleID, privRoleID int
	t.Cleanup(func() {
		if limitedID != 0 {
			db.Exec("DELETE FROM staff_roles WHERE staff_id = ?", limitedID)
			db.Exec("DELETE FROM staff WHERE id = ?", limitedID)
		}
		if victimID != 0 {
			db.Exec("DELETE FROM staff_roles WHERE staff_id = ?", victimID)
			db.Exec("DELETE FROM staff WHERE id = ?", victimID)
		}
		if roleID != 0 {
			db.Exec("DELETE FROM role_permissions WHERE role_id = ?", roleID)
			db.Exec("DELETE FROM staff_roles WHERE role_id = ?", roleID)
			db.Exec("DELETE FROM roles WHERE id = ?", roleID)
		}
		if privRoleID != 0 {
			db.Exec("DELETE FROM role_permissions WHERE role_id = ?", privRoleID)
			db.Exec("DELETE FROM staff_roles WHERE role_id = ?", privRoleID)
			db.Exec("DELETE FROM roles WHERE id = ?", privRoleID)
		}
	})

	// 1) 超管建一个受限角色：含 staff/role 相关权限，但绝不含 billing:manage。
	limitedPerms := []string{"staff:create", "staff:edit", "role:create", "role:view"}
	roleID = createRole(t, r, admin, roleName, limitedPerms)

	// 2) 超管建一个高权角色：含 billing:manage（用于 A3 越权赋角色）。
	privRoleID = createRole(t, r, admin, privRoleName, []string{"billing:manage"})

	// 3) 超管建两个普通 staff（is_superuser=false），并给受限管理员赋受限角色。
	createUser(t, r, admin, limitedName, "pass123", false)
	createUser(t, r, admin, victimName, "pass123", false)
	limitedID = staffIDByUsername(t, limitedName)
	victimID = staffIDByUsername(t, victimName)
	assignRoles(t, r, admin, limitedID, []int{roleID})

	// 受限管理员登录换令牌。
	limitedTok := login(t, r, limitedName, "pass123")

	// --- S1：铸造越权角色 ---
	// 含自身没有的 billing:manage → 403。
	wDeny := doJSON(r, "POST", "/api/v1/role/create", limitedTok, map[string]interface{}{
		"name":        "rbac_mint_deny_" + suffix,
		"permissions": []string{"role:view", "billing:manage"},
	})
	require.Equal(t, http.StatusForbidden, wDeny.Code, "S1: 铸造含自身没有权限的角色应 403，实际: %s", wDeny.Body.String())

	// 只含自身子集(role:view) → 200，并清理造出来的角色。
	wOK := doJSON(r, "POST", "/api/v1/role/create", limitedTok, map[string]interface{}{
		"name":        "rbac_mint_ok_" + suffix,
		"permissions": []string{"role:view"},
	})
	require.Equal(t, http.StatusOK, wOK.Code, "S1: 铸造自身权限子集的角色应 200，实际: %s", wOK.Body.String())
	mintedID := int(decode(t, wOK).Data.(map[string]interface{})["id"].(float64))
	t.Cleanup(func() {
		db.Exec("DELETE FROM role_permissions WHERE role_id = ?", mintedID)
		db.Exec("DELETE FROM staff_roles WHERE role_id = ?", mintedID)
		db.Exec("DELETE FROM roles WHERE id = ?", mintedID)
	})

	// --- A2：创建时自封超管 ---
	// 受限管理员创建 staff 传 is_superuser:true → 200 但落库/响应均为 false。
	newStaffName := "rbac_a2_" + suffix
	wA2 := doJSON(r, "POST", "/api/v1/staff/create", limitedTok, map[string]interface{}{
		"username":     newStaffName,
		"password":     "pass123",
		"is_active":    true,
		"is_superuser": true,
	})
	require.Equal(t, http.StatusOK, wA2.Code, "A2: 受限管理员创建 staff 应 200，实际: %s", wA2.Body.String())
	a2Data := decode(t, wA2).Data.(map[string]interface{})
	require.False(t, a2Data["is_superuser"].(bool), "A2: 响应 is_superuser 应被强制为 false")
	a2ID := int(a2Data["id"].(float64))
	t.Cleanup(func() {
		db.Exec("DELETE FROM staff_roles WHERE staff_id = ?", a2ID)
		db.Exec("DELETE FROM staff WHERE id = ?", a2ID)
	})
	require.False(t, staffIsSuperuser(t, a2ID), "A2: 落库 staff.is_superuser 应为 false")

	// --- A1：更新时提超管位 ---
	// 受限管理员 PUT 普通员工传 is_superuser:true → 目标仍 false。
	wA1 := doJSON(r, "PUT", fmt.Sprintf("/api/v1/staff/update/%d", victimID), limitedTok, map[string]interface{}{
		"is_superuser": true,
	})
	require.Equal(t, http.StatusOK, wA1.Code, "A1: 受限管理员更新普通员工应 200，实际: %s", wA1.Body.String())
	require.False(t, staffIsSuperuser(t, victimID), "A1: 目标 staff.is_superuser 应仍为 false")

	// --- A3：赋越权角色 ---
	// 受限管理员 PUT 给员工 role_ids 指向含 billing:manage 的高权角色 → 403。
	wA3 := doJSON(r, "PUT", fmt.Sprintf("/api/v1/staff/update/%d", victimID), limitedTok, map[string]interface{}{
		"role_ids": []int{privRoleID},
	})
	require.Equal(t, http.StatusForbidden, wA3.Code, "A3: 赋含自身未拥有权限的角色应 403，实际: %s", wA3.Body.String())
}

// --- 本用例局部辅助（apptest 包内，避免与既有 helper 重名；Task 3 复用这些，勿重复定义）---

// createRole 以超管令牌经真实 HTTP 建角色，返回其 ID。
func createRole(t *testing.T, r *gin.Engine, adminTok, name string, perms []string) int {
	t.Helper()
	w := doJSON(r, "POST", "/api/v1/role/create", adminTok, map[string]interface{}{
		"name":        name,
		"permissions": perms,
	})
	require.Equal(t, http.StatusOK, w.Code, "创建角色失败: %s", w.Body.String())
	return int(decode(t, w).Data.(map[string]interface{})["id"].(float64))
}

// assignRoles 以超管令牌给指定 staff 赋角色（经 PUT /staff/update/:id 的 role_ids）。
func assignRoles(t *testing.T, r *gin.Engine, adminTok string, staffID int, roleIDs []int) {
	t.Helper()
	w := doJSON(r, "PUT", fmt.Sprintf("/api/v1/staff/update/%d", staffID), adminTok, map[string]interface{}{
		"role_ids": roleIDs,
	})
	require.Equal(t, http.StatusOK, w.Code, "赋角色失败: %s", w.Body.String())
}

// staffIDByUsername 直查 staff 表拿 ID（唯一用户名）。Task 3 复用此签名。
func staffIDByUsername(t *testing.T, username string) int {
	t.Helper()
	var id int
	require.NoError(t, framework.DB.Raw("SELECT id FROM staff WHERE username = ?", username).Scan(&id).Error)
	require.NotZero(t, id, "未找到 staff: %s", username)
	return id
}

// staffIsSuperuser 直查 staff 表的 is_superuser。
func staffIsSuperuser(t *testing.T, id int) bool {
	t.Helper()
	var v bool
	require.NoError(t, framework.DB.Raw("SELECT is_superuser FROM staff WHERE id = ?", id).Scan(&v).Error)
	return v
}
```

> `createRole` / `assignRoles` / `staffIDByUsername` / `staffIsSuperuser` 为本文件包级辅助，**Task 3 直接复用、勿重复定义**（同一 `apptest` 包内重名会编译失败）。

- [ ] **Step 2: 运行验证失败** —— Run:
```
go test ./apptest/ -run TestStaffRBAC_MintAndEscalationGuards -v
```
Expected：此为回归任务，授权收敛层已落地，正确写法下应直接 PASS。若授权收敛层被回退（假想），会在 S1/A2/A1/A3 断言处 FAIL：S1 应得 403 却得 200、A2 落库 `is_superuser=true`、A1 目标变超管、A3 应得 403 却得 200。

- [ ] **Step 3: 确认已有实现满足** —— 无需改生产码。已 Read 核对到位：
  - S1：`role_service.go` `CreateChecked`（api.go `CreateRole` 唯一入口）→ 非超管走 `assertPermsWithinActor`，权限项不在操作者并集内即 `apperr.Forbidden`（→403）；子集通过。
  - A2：`service.go` `CreateStaffChecked`（api.go `CreateStaff` 唯一入口）→ 非超管强制 `req.IsSuperuser = false`，再落库；响应经 `dbToStaff` 取库值。
  - A1：`service.go` `UpdateStaffChecked`（api.go `UpdateStaff` 唯一入口）→ 非超管 `req.IsSuperuser = nil`（`is_superuser` 不写入 updates），目标超管位不变。
  - A3：`service.go` `UpdateStaffChecked` → 非超管且 `req.RoleIDs != nil` 走 `assertRolesWithinActor`，目标角色权限超出操作者并集即 `apperr.Forbidden`（→403）。
  - 路由/权限：`module.go` 中 `role:create`/`staff:create`/`staff:edit` 门控；受限角色含这三者故过中间件，越权判定由 service 层完成。

- [ ] **Step 4: 运行验证通过** —— Run:
```
go test ./apptest/ -run TestStaffRBAC_MintAndEscalationGuards -v
```
Expected：PASS（S1 deny=403 / ok=200，A2 响应与落库均 false，A1 目标仍 false，A3=403）。同时跑 `go vet ./apptest/` 无告警、`go test ./apptest/` 全绿（确认未污染共享表）。

- [ ] **Step 5: 提交** —— Run:
```
git add apptest/staff_rbac_test.go
git commit -m "test(staff): 后台 RBAC 越权铸造/提权回归覆盖 S1/A1/A2/A3"
```
---

### Task 3: RBAC 删除守卫 + 禁用即失效令牌回归（A5/A4/X2）

**类型**：回归防护（fix 已确认存在：`DeleteStaffChecked` 的自删/非超管删超管/最后一个超管三重守卫，及前台 `SetActive` 禁用递增 `token_version`；测试应立即通过）。

**Files:**
- Test（追加到 Task 2 已建的文件）：`backend/apptest/staff_rbac_test.go`（`package apptest`，复用现有 helper 与 Task 2 的 `staffIDByUsername`/`createRole` 等，勿重复 import/定义）
- 被覆盖生产码（只读核对，不改）：
  - `backend/modules/staff/internal/service.go:388` `func (s *serviceImpl) DeleteStaffChecked(id, actorID int, actorSuper bool) error`
  - `backend/modules/staff/internal/api.go:290` `DeleteStaff`（DELETE `/api/v1/staff/delete/:id`，`PermissionMiddleware("staff:delete")` → `DeleteStaffChecked(id, c.GetInt("userID"), c.GetBool("is_superuser"))`）
  - `backend/modules/staff/internal/module.go:84` 路由；`middleware.go` `PermissionMiddleware`（超管放行；非超管须持 `staff:delete`，`is_superuser` 上下文变量对非超管不设置 → `actorSuper=false`）
  - `backend/modules/user/internal/service.go` `SetActive`（禁用递增 `token_version`）/`Authenticate`（`!IsActive` → `apperr.Forbidden`）；`middleware.go` `UserAuth`（比对 `token_version`，不符→401）；`module.go:44` PUT `/api/v1/admin/users/:id/status`（`user:manage`）；`admin_api.go` `AdminSetUserStatus` 读 `AdminStatusRequest{IsActive *bool json:"is_active" binding:"required"}`

**Interfaces:**
- Consumes（既有 helper，签名固定）：`setupRouter() *gin.Engine`；`doJSON(r,method,path,token string,body interface{}) *httptest.ResponseRecorder`；`decode(t,w) framework.Response`；`adminToken(t,r) string`；`login(t,r,account,password) string`；`createUser(t,r,adminTok,username,password string,superuser bool)`；`registerUser(t,r,phone string) string`；`framework.DB *gorm.DB`。
- Produces（新增测试函数）：`TestDeleteStaffGuards_HTTP(t)`、`TestDisabledUserTokenAndLoginRejected(t)`。

关键事实（已 Read 核对）：后台员工表 `TableName()="staff"`（列 `id/username/is_superuser`），角色三表 `roles`/`role_permissions`（列 `role_id`,`permission`）/`staff_roles`（列 `staff_id`,`role_id`）；权限标识串 `"staff:delete"`；删除路由 `DELETE /api/v1/staff/delete/:id`。业务错误经 `framework.FailErr` 映射：`Forbidden→403`、`Conflict→409`。种子仅 `admin` 一个超管（`admin/admin123`），`test` 非超管。**绝不 CleanTable("staff") 等共享表**：所有造行用唯一命名 + `t.Cleanup` 按 id 删除。

- [ ] **Step 1: 写失败测试** —— 新建 `backend/apptest/staff_rbac_test.go`：

```go
package apptest

import (
	"fmt"
	"net/http"
	"testing"

	"manager-backend/framework"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// 注：staffIDByUsername 已在 Task 2 的 staff_rbac_test.go 中定义（签名 (t *testing.T, username string) int），
// 本任务追加到同一文件，直接复用，勿重复定义。

// grantStaffDelete 直接经原生 SQL 给某员工挂一个仅含 staff:delete 权限的角色（造数据，非授权路径）。
// 返回清理函数，删除本次造出的 role/role_permissions/staff_roles 行。
func grantStaffDelete(t *testing.T, staffID int, roleName string) func() {
	t.Helper()
	db := framework.DB
	require.NoError(t, db.Exec(
		"INSERT INTO roles (name, description, is_builtin) VALUES (?, '', ?)", roleName, false).Error)
	var roleID int
	require.NoError(t, db.Table("roles").Where("name = ?", roleName).Pluck("id", &roleID).Error)
	require.NotZero(t, roleID)
	require.NoError(t, db.Exec(
		"INSERT INTO role_permissions (role_id, permission) VALUES (?, ?)", roleID, "staff:delete").Error)
	require.NoError(t, db.Exec(
		"INSERT INTO staff_roles (staff_id, role_id) VALUES (?, ?)", staffID, roleID).Error)
	return func() {
		db.Exec("DELETE FROM staff_roles WHERE role_id = ?", roleID)
		db.Exec("DELETE FROM role_permissions WHERE role_id = ?", roleID)
		db.Exec("DELETE FROM roles WHERE id = ?", roleID)
	}
}

// A5：DeleteStaffChecked 删除守卫（HTTP 层，经 DELETE /api/v1/staff/delete/:id）。
// 受限管理员 = 非超管但持 staff:delete，通过 PermissionMiddleware 后由 *Checked 拦截越权删除。
func TestDeleteStaffGuards_HTTP(t *testing.T) {
	r := setupRouter()
	admin := adminToken(t, r)

	// 造一个受限管理员（非超管，稍后挂 staff:delete）
	restricted := "rbac_del_actor"
	createUser(t, r, admin, restricted, "pass123", false)
	restrictedID := staffIDByUsername(t, restricted)
	t.Cleanup(func() { framework.DB.Exec("DELETE FROM staff WHERE username = ?", restricted) })

	cleanupRole := grantStaffDelete(t, restrictedID, "rbac_del_role")
	t.Cleanup(cleanupRole)

	actorTok := login(t, r, restricted, "pass123")

	// A5-a：受限管理员删超管（种子 admin 是超管）→ 403（非超管无权删超管）。
	adminID := staffIDByUsername(t, "admin")
	wa := doJSON(r, "DELETE", fmt.Sprintf("/api/v1/staff/delete/%d", adminID), actorTok, nil)
	assert.Equal(t, http.StatusForbidden, wa.Code, "受限管理员删超管应 403: %s", wa.Body.String())

	// A5-b：受限管理员删自己 → 403（禁止自删）。
	wb := doJSON(r, "DELETE", fmt.Sprintf("/api/v1/staff/delete/%d", restrictedID), actorTok, nil)
	assert.Equal(t, http.StatusForbidden, wb.Code, "删自己应 403: %s", wb.Body.String())

	// A5-c（末位超管守卫的可达半边）：超管 admin 删一个“非末位”超管 → 成功（count>1，守卫放行）。
	// 说明：count<=1 的 409 分支在生产 HTTP 下不可达（唯一超管即操作者自身，先被自删守卫拦截），
	// 且 apptest 不得截断/降级共享的 admin 超管行；此处断言超管可删非末位超管这一守卫放行路径。
	secondSuper := "rbac_second_super"
	createUser(t, r, admin, secondSuper, "pass123", true) // admin 为超管 → CreateStaffChecked 保留 is_superuser=true
	secondSuperID := staffIDByUsername(t, secondSuper)
	t.Cleanup(func() { framework.DB.Exec("DELETE FROM staff WHERE username = ?", secondSuper) })

	wc := doJSON(r, "DELETE", fmt.Sprintf("/api/v1/staff/delete/%d", secondSuperID), admin, nil)
	assert.Equal(t, http.StatusOK, wc.Code, "超管删非末位超管应成功: %s", wc.Body.String())
	// 已删除：库中该行应消失。
	var cnt int64
	framework.DB.Table("staff").Where("id = ?", secondSuperID).Count(&cnt)
	assert.Equal(t, int64(0), cnt, "非末位超管应已被删除")
}

// A4/X2：禁用前台用户后，其旧 user 令牌立即失效（token_version 递增）且无法再登录。
func TestDisabledUserTokenAndLoginRejected(t *testing.T) {
	const phone = "13900050051" // 唯一手机号，避免污染共享 users 表
	t.Cleanup(func() { framework.DB.Exec("DELETE FROM users WHERE phone = ?", phone) })

	r := setupRouter()
	admin := adminToken(t, r)

	userTok := registerUser(t, r, phone) // 密码固定 "pass123"
	// 前置健壮性：禁用前旧令牌可用。
	require.Equal(t, http.StatusOK, doJSON(r, "GET", "/api/v1/user/me", userTok, nil).Code)

	// 取前台用户 id（users 表）。
	var userID int
	require.NoError(t, framework.DB.Table("users").Where("phone = ?", phone).Pluck("id", &userID).Error)
	require.NotZero(t, userID)

	// adminToken PUT 禁用：is_active=false（AdminStatusRequest.IsActive 为 *bool binding:"required"）。
	wd := doJSON(r, "PUT", fmt.Sprintf("/api/v1/admin/users/%d/status", userID), admin,
		map[string]interface{}{"is_active": false})
	require.Equal(t, http.StatusOK, wd.Code, "禁用应成功: %s", wd.Body.String())

	// X2：禁用后旧 user 令牌打 /user/me → 401（token_version 不匹配，UserAuth 拒绝）。
	assert.Equal(t, http.StatusUnauthorized,
		doJSON(r, "GET", "/api/v1/user/me", userTok, nil).Code, "禁用后旧令牌应失效")

	// A4：被禁用手机号再登录 → 403（Authenticate 对 !IsActive 返回 Forbidden）。
	wl := doJSON(r, "POST", "/api/v1/user/auth/login", "", map[string]string{
		"phone": phone, "password": "pass123",
	})
	assert.Equal(t, http.StatusForbidden, wl.Code, "被禁用账号登录应 403: %s", wl.Body.String())
}
```

- [ ] **Step 2: 运行验证失败** —— Run：
  `cd backend && go test ./apptest/ -run 'TestDeleteStaffGuards_HTTP|TestDisabledUserTokenAndLoginRejected' -v`
  Expected：若守卫/失效逻辑符合预期，本回归应直接 PASS；若测试基建有误（如表名/字段/路由拼错、`framework.DB` 未初始化）则先 FAIL——常见首失败点为断言处的具体 Code 与实现不符（如某守卫未映射到 403/409、或禁用未递增 `token_version` 导致 `/user/me` 返回 200 而非 401）。回归任务预期无生产缺陷，故此步用于确认测试自身可编译且能真正命中路由（非 404）。

- [ ] **Step 3: 确认已有实现满足（回归任务，不改生产码）** —— 只读核对以下要点已就位：
  - `service.go:388` `DeleteStaffChecked`：`id==actorID` → `apperr.Forbidden("不能删除当前登录账号")`；`target.IsSuperuser && !actorSuper` → `apperr.Forbidden("无权删除超级管理员")`；`target.IsSuperuser && actorSuper && countSuperusers()<=1` → `apperr.Conflict("不能删除最后一个超级管理员")`；否则 `DeleteStaff`。
  - `api.go:290` `DeleteStaff` 传 `c.GetInt("userID")`（actor id）与 `c.GetBool("is_superuser")`（受限管理员为 false）。`middleware.go` `PermissionMiddleware("staff:delete")`：超管放行且置 `is_superuser=true`；非超管须持 `staff:delete` 才 `c.Next()`（否则 403，不进 handler）。
  - `user/internal/service.go` `SetActive(id,false)` 调 `repo.bumpTokenVersion(id)`；`Authenticate` 对 `!c.IsActive` 返回 `apperr.Forbidden("账号已被禁用")`；`middleware.go` `UserAuth` 的 `Validate` 比对 `CurrentTokenVersion`，不符返回 error → 框架映射 401。
  - `user/internal/admin_api.go` `AdminSetUserStatus` 绑定 `AdminStatusRequest{IsActive *bool json:"is_active" binding:"required"}`，缺字段 → 400。路由 `module.go:44` PUT `/admin/users/:id/status` 挂 `user:manage`（admin 为超管放行）。
  若 Step 2 出现非预期 FAIL，按“测试补测暴露生产 bug 则直接修 + 逐条记录”处理：优先核验断言的 HTTP 码与 `apperr` 种类映射，不得为迁就测试而放宽守卫。

- [ ] **Step 4: 运行验证通过** —— Run：
  `cd backend && go test ./apptest/ -run 'TestDeleteStaffGuards_HTTP|TestDisabledUserTokenAndLoginRejected' -v`
  再跑整包确认无串扰：`cd backend && go test ./apptest/`
  Expected：PASS（两用例均通过，且未截断共享表，重复运行仍稳定——造行均按唯一命名 + `t.Cleanup` 清理）。

- [ ] **Step 5: 提交** —— 
  `git add backend/apptest/staff_rbac_test.go`
  commit message：
  `test(staff): 回归 RBAC 删除守卫与禁用即失效令牌（A5/A4/X2）`
---

### Task 4: 前台改 status 越权绕过席位计费回归（I1/X10）

**类型**：回归防护(fix 已确认存在，测试应立即通过——`updateFields` 白名单不含 `status`，见 `modules/phone/internal/service.go:452-470`)

**Files:**
- Test（新建）：`/home/root/workspace005/gloryphone-code/backend/apptest/phone_security_test.go`

**Interfaces:**
- Consumes（apptest 既有 helper / 全局，勿重定义）：
  - `setupRouter() *gin.Engine`
  - `doJSON(r *gin.Engine, method, path, token string, body interface{}) *httptest.ResponseRecorder`
  - `decode(t *testing.T, w *httptest.ResponseRecorder) framework.Response`（`framework.Response{Code int, Message string, Data interface{}}`，成功 Code=0）
  - `registerUser(t *testing.T, r *gin.Engine, phone string) string`（注册手机号，密码固定 `pass123`，返回前台 user token）
  - `framework.DB *gorm.DB`（跑原生 SQL 造 seat / 查 & 断言 `cloud_phones.status`）
- 被验证的生产行为：`PUT /api/v1/phone/update/:id` → `UpdateCloudPhone`(`modules/phone/internal/api.go:124`) → `serviceImpl.Update`(`service.go:383`) → `updateFields`(`service.go:452`) 白名单**不含 status**，故前台传 `status` 被静默丢弃。
- Produces（本任务新增测试函数）：`TestPhoneUpdateStatusMassAssignmentBlockedHTTP`

- [ ] **Step 1: 写失败测试** —— 完整文件（新建 `apptest/phone_security_test.go`）：

```go
package apptest

import (
	"fmt"
	"net/http"
	"testing"
	"time"

	"manager-backend/framework"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// I1/X10 回归：前台经通用 /phone/update 把 status 篡改为 RECYCLED 应被服务端白名单丢弃，
// 否则可让实例逃过 listNonRecycledByUser 的席位统计（绕过席位计费）或让回收站实例逃逸清理。
// 真实 HTTP：registerUser → 直接 INSERT 一个 active seat → 建机 → PUT update{status:RECYCLED}
// → 200 且响应 data.status 保持服务端值；再查 cloud_phones 该行 status 列仍为原值（未被篡改）。
func TestPhoneUpdateStatusMassAssignmentBlockedHTTP(t *testing.T) {
	r := setupRouter()

	// 唯一手机号，避免撞库（不截断共享表）。
	phone := "13900920401"
	token := registerUser(t, r, phone)

	// 由手机号回查前台用户 ID（apptest 不能 import user internal，走 framework.DB 原生查 users 表）。
	var userID int
	require.NoError(t, framework.DB.Raw("SELECT id FROM users WHERE phone = ?", phone).Scan(&userID).Error)
	require.NotZero(t, userID, "注册后应能按手机号查到用户 ID")

	// 建机需要席位：直接 INSERT 一个未过期的 active seat 授权单元（billing_license_units）。
	// 该用户初始非回收实例数 0 < 容量 1 → 允许建 1 台。
	now := time.Now()
	require.NoError(t, framework.DB.Exec(
		`INSERT INTO billing_license_units
		 (user_id, kind, status, current_instance_id, source, source_ref, order_item_id, expire_at, created_at, updated_at)
		 VALUES (?, 'seat', 'active', '', 'grant', 'test:I1', 0, ?, ?, ?)`,
		userID, now.Add(24*time.Hour), now, now,
	).Error)
	t.Cleanup(func() {
		framework.DB.Exec("DELETE FROM billing_license_units WHERE user_id = ?", userID)
	})

	// 建机（无中台配置 → 走本地档案，直接置 CREATED；不触中台）。唯一 name。
	name := fmt.Sprintf("I1机-%d", now.UnixNano())
	w := doJSON(r, "POST", "/api/v1/phone/create", token, map[string]interface{}{"name": name})
	require.Equal(t, http.StatusOK, w.Code, "建机应 200: %s", w.Body.String())
	created := decode(t, w).Data.(map[string]interface{})
	phoneID := int(created["id"].(float64))
	require.NotZero(t, phoneID)
	initialStatus := created["status"].(string) // 本地降级建机 → CREATED
	t.Cleanup(func() {
		framework.DB.Exec("DELETE FROM cloud_phones WHERE id = ?", phoneID)
	})
	require.Equal(t, "CREATED", initialStatus, "本地降级建机初始状态应为 CREATED")

	// 越权尝试：通用 update 携带 status:RECYCLED（外加合法 name 改动，确保确实进了 update 分支）。
	uw := doJSON(r, "PUT", fmt.Sprintf("/api/v1/phone/update/%d", phoneID), token, map[string]interface{}{
		"name":   name + "-改名",
		"status": "RECYCLED",
	})
	require.Equal(t, http.StatusOK, uw.Code, "update 应 200: %s", uw.Body.String())
	resp := decode(t, uw)
	require.Equal(t, 0, resp.Code)
	updated := resp.Data.(map[string]interface{})

	// 断言①：响应 data.status 未被前台篡改为 RECYCLED，保持服务端状态机的值（原始 CREATED）。
	assert.NotEqual(t, "RECYCLED", updated["status"], "前台不得通过 update 把 status 置为 RECYCLED")
	assert.Equal(t, initialStatus, updated["status"], "status 应保持服务端值，未被篡改")
	// 合法字段 name 应生效，证明 update 确实执行、仅 status 被白名单丢弃。
	assert.Equal(t, name+"-改名", updated["name"], "合法字段 name 应更新成功")

	// 断言②：直查 cloud_phones 该行 status 列仍为原值（DB 层未被写脏）。
	var dbStatus string
	require.NoError(t, framework.DB.Raw("SELECT status FROM cloud_phones WHERE id = ?", phoneID).Scan(&dbStatus).Error)
	assert.Equal(t, initialStatus, dbStatus, "DB 中 status 列不应被前台 update 篡改")
	assert.NotEqual(t, "RECYCLED", dbStatus)
}
```

- [ ] **Step 2: 运行验证失败** —— Run:
  ```bash
  go test ./apptest/ -run TestPhoneUpdateStatusMassAssignmentBlockedHTTP -v
  ```
  Expected：本任务为回归防护，`updateFields` 白名单已不含 `status`，故**预期直接 PASS**。若此步意外 FAIL，说明白名单被回退（`updateFields` 混入了 `fields["status"]=req.Status`），断言会在 `assert.NotEqual(..., updated["status"])` / `dbStatus` 处报 `RECYCLED != CREATED` 命中 bug。

- [ ] **Step 3: 确认已有实现满足（回归任务，不改生产码）** —— 需 Read 核对的要点：
  - `modules/phone/internal/service.go:452-470` `updateFields`：仅收敛 `name / image_id / proxy_id / remark`，**故意不含 `status`**（注释 `service.go:457-459` 明确说明：前台通用 update 改 status 可绕过席位计费，故禁写）。
  - `modules/phone/internal/service.go:383-391` `Update`：先属主校验（`GetByID`），再 `repo.update(userID, id, updateFields(req))`，最后 `GetByIDDisplay` 返回；无中台时 `resolveLiveStatuses` 直接返回，`data.status` 即本地档案值。
  - `modules/phone/internal/model.go:127-134` `CloudPhoneUpdate` 含 `Status string \`json:"status"\``（请求体能带上，但服务端丢弃），表名 `cloud_phones`（`model.go:98`），status 列默认 `CREATED`（`model.go:77`）。
  - `modules/billing/internal/license_model.go:10-24`：`billing_license_units` NOT NULL 列为 `user_id/kind/source/expire_at`，`status` 默认 `active`——Step 1 的 INSERT 已显式给全。
  确认无需改动任何生产文件。

- [ ] **Step 4: 运行验证通过** —— Run:
  ```bash
  go test ./apptest/ -run TestPhoneUpdateStatusMassAssignmentBlockedHTTP -v && go test ./apptest/
  ```
  Expected：PASS（新用例通过，且不污染共享表——`t.Cleanup` 已清理自造的 seat 与 cloud_phones 行）。

- [ ] **Step 5: 提交** ——
  ```bash
  git add backend/apptest/phone_security_test.go
  git commit -m "test(phone): 回归前台 update 改 status 越权绕过席位计费（I1/X10）"
  ```
---

### Task 5: 云手机绑定代理的属主/存在性校验（I2/X11）—— 发现并修复

**类型**：发现并修复（当前 `modules/phone/internal/service.go` 的 `Create`(L333) 与 `Update`(L383) 把 `req.ProxyID` 直接入库，从不校验该代理是否存在、是否属于当前用户——任意前台用户可把他人代理 ID 绑到自己机器上，属 BOLA/IDOR。测试先失败暴露，再改生产码使其通过。）

**Files:**
- Test: `apptest/phone_security_test.go`（新建，`package apptest`；不 import 任何模块 internal）
- Modify: `modules/proxy/proxy.go`（新增公开门面 `GetByID`，委托 `proxyinternal.ProxyService.GetByID`，L50 `Delete` 之后追加）
- Modify: `modules/phone/internal/service.go`（`import` 块 L12 后加 `"manager-backend/modules/proxy"`；`Create` 在构造 `item` 前、`Update` 在 `s.repo.update` 前各加一段代理校验）

**Interfaces:**
- Consumes（既有，勿重定义）：`setupRouter() *gin.Engine`、`doJSON(r,method,path,token,body) *httptest.ResponseRecorder`、`decode(t,w) framework.Response`、`registerUser(t,r,phone string) string`；`proxyinternal.ProxyService.GetByID(userID, id int) (*Proxy, error)`（非属主/不存在返回 `apperr.NotFound("代理不存在")`）。
- Produces：测试函数 `TestPhoneBindProxyOwnershipHTTP`；新公开门面 `proxy.GetByID(userID, id int) (*proxy.Proxy, error)`；`phone` 服务 `Create`/`Update` 内新增代理校验分支。

- [ ] **Step 1: 写失败测试** —— 新建 `apptest/phone_security_test.go`：

```go
package apptest

import (
	"fmt"
	"net/http"
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// createProxyFor 为某前台用户建一条代理，返回其 ID（唯一命名，避免污染共享表）。
func createProxyFor(t *testing.T, r *gin.Engine, token, name, host string) int {
	t.Helper()
	w := doJSON(r, "POST", "/api/v1/proxy/create", token, map[string]interface{}{
		"name": name, "host": host, "port": 1080,
	})
	require.Equal(t, http.StatusOK, w.Code, "建代理失败: %s", w.Body.String())
	return int(decode(t, w).Data.(map[string]interface{})["id"].(float64))
}

// TestPhoneBindProxyOwnershipHTTP 云手机绑定代理必须校验属主+存在性（I2/BOLA）：
//   - userA 建机传 userB 的 proxyID → 应被拒（404，隐藏他人资源），且不得入库；
//   - userA 建机（不带代理）后 update 传 userB 的 proxyID → 应被拒（404）；
//   - 对照：userA 绑自己的 proxyA → 成功且 proxy_id 生效。
// 中台未配置（apptest 降级）时 Create 走本地分支直接落库，正好聚焦「绑定校验」这层。
func TestPhoneBindProxyOwnershipHTTP(t *testing.T) {
	r := setupRouter()
	tokenA := registerUser(t, r, "13900021001")
	tokenB := registerUser(t, r, "13900021002")

	proxyA := createProxyFor(t, r, tokenA, "phonesec-proxyA", "203.0.113.21")
	proxyB := createProxyFor(t, r, tokenB, "phonesec-proxyB", "203.0.113.22")

	// A 建机绑 B 的代理 → 拒绝（404），且不落库。
	w := doJSON(r, "POST", "/api/v1/phone/create", tokenA, map[string]interface{}{
		"name": "phonesec-A-badbind", "proxy_id": proxyB,
	})
	assert.Equal(t, http.StatusNotFound, w.Code,
		"越权绑定他人代理必须被拒(404)，实际=%d body=%s", w.Code, w.Body.String())

	// A 建机（不带代理）→ 成功。
	w = doJSON(r, "POST", "/api/v1/phone/create", tokenA, map[string]interface{}{
		"name": "phonesec-A-phone",
	})
	require.Equal(t, http.StatusOK, w.Code, w.Body.String())
	phoneID := int(decode(t, w).Data.(map[string]interface{})["id"].(float64))

	// A 把该机 update 绑 B 的代理 → 拒绝（404）。
	w = doJSON(r, "PUT", fmt.Sprintf("/api/v1/phone/update/%d", phoneID), tokenA, map[string]interface{}{
		"proxy_id": proxyB,
	})
	assert.Equal(t, http.StatusNotFound, w.Code,
		"越权 update 绑他人代理必须被拒(404)，实际=%d body=%s", w.Code, w.Body.String())

	// 对照：A 绑自己的 proxyA → 成功且生效。
	w = doJSON(r, "PUT", fmt.Sprintf("/api/v1/phone/update/%d", phoneID), tokenA, map[string]interface{}{
		"proxy_id": proxyA,
	})
	require.Equal(t, http.StatusOK, w.Code, w.Body.String())
	assert.Equal(t, float64(proxyA), decode(t, w).Data.(map[string]interface{})["proxy_id"],
		"绑自己的代理应成功生效")

	// 对照：A 建机时直接绑自己的 proxyA → 成功。
	w = doJSON(r, "POST", "/api/v1/phone/create", tokenA, map[string]interface{}{
		"name": "phonesec-A-goodbind", "proxy_id": proxyA,
	})
	require.Equal(t, http.StatusOK, w.Code, w.Body.String())
	assert.Equal(t, float64(proxyA), decode(t, w).Data.(map[string]interface{})["proxy_id"])
}
```

（注意：`createProxyFor` 用到 `*gin.Engine`，需在该文件 import `"github.com/gin-gonic/gin"`；`fmt`/`net/http`/`testing`/`assert`/`require` 见上方 import 块。唯一手机号 `139000210xx`、唯一代理/机器名 `phonesec-*` 避免撞库；这些行随机器/代理创建，测试造的数据不截断共享表，串行运行即可。）

- [ ] **Step 2: 运行验证失败** —— Run：
  `go test ./apptest/ -run TestPhoneBindProxyOwnershipHTTP -v`
  Expected：**FAIL**。当前 `Create`/`Update` 不校验代理归属，A 传 `proxy_id=proxyB` 会成功入库并返回 `200`，第一个 `assert.Equal(http.StatusNotFound, w.Code)` 失败（实际 200），暴露 I2 越权绑定。

- [ ] **Step 3: 实现/修复** ——

在 `modules/proxy/proxy.go` 末尾（L50 `Delete` 函数之后）追加公开门面：

```go
// GetByID 返回某用户名下指定 id 的代理；不存在或非属主一律 apperr.NotFound（隐藏他人资源）。
// 供跨模块（如云手机绑定代理）做属主 + 存在性校验，杜绝 BOLA/IDOR。
func GetByID(userID, id int) (*Proxy, error) {
	return proxyinternal.ProxyService.GetByID(userID, id)
}
```

（`proxyinternal.ProxyService.GetByID` 已存在：`repo.findByID` 以 `user_id + id` 双约束查，查不到即 `apperr.NotFound("代理不存在")`，天然覆盖「不存在」与「他人的」两种情形，映射 404。无需改 internal service/repository。）

在 `modules/phone/internal/service.go` 的 import 块（现有 `"manager-backend/modules/billing"` 那段，L12 附近）追加：

```go
	"manager-backend/modules/proxy"
```

（`phone → proxy` 单向依赖，`proxy` 不 import `phone`，无环；与既有 `phone → billing` 同构。）

`Create`：在席位校验之后、构造 `item` 之前插入代理校验（现 L337 与 L338 之间）：

```go
	if err := s.checkSeatAvailable(userID); err != nil {
		return nil, err
	}
	// 绑定代理必须是本人已存在的代理（否则 404，堵 BOLA/I2）。
	if req.ProxyID != 0 {
		if _, err := proxy.GetByID(userID, int(req.ProxyID)); err != nil {
			return nil, err
		}
	}
	item := CloudPhone{
```

`Update`：在属主校验之后、`s.repo.update` 之前插入（现 L384-L387 之间）：

```go
func (s *serviceImpl) Update(userID, id int, req *CloudPhoneUpdate) (*CloudPhone, error) {
	if _, err := s.GetByID(userID, id); err != nil {
		return nil, err
	}
	// 改绑代理必须是本人已存在的代理（否则 404，堵 BOLA/I2）。
	if req.ProxyID != 0 {
		if _, err := proxy.GetByID(userID, int(req.ProxyID)); err != nil {
			return nil, err
		}
	}
	if err := s.repo.update(userID, id, updateFields(req)); err != nil {
		return nil, err
	}
	return s.GetByIDDisplay(userID, id)
}
```

（`req.ProxyID` 为 `uint`，`proxy.GetByID` 收 `int`，故显式 `int(req.ProxyID)`；`ProxyID==0` 表示不绑定/不改绑，跳过校验，保留「解绑留 0」语义。`err` 直接返回，经 handler `framework.FailErr` 把 `apperr.NotFound` 映射为 404。）

- [ ] **Step 4: 运行验证通过** —— Run：
  `go test ./apptest/ -run TestPhoneBindProxyOwnershipHTTP -v`
  再全量回归确保未破坏边界与既有用例：
  `go build ./... && go test ./framework/ -run TestModuleBoundaries && go test ./apptest/ ./modules/phone/... ./modules/proxy/...`
  Expected：**PASS**（越权绑定被拒 404，绑自己代理成功；模块边界仍成立）。

- [ ] **Step 5: 提交** ——
  ```bash
  git add apptest/phone_security_test.go modules/proxy/proxy.go modules/phone/internal/service.go
  git commit -m "fix(phone): 绑定代理校验属主+存在性，堵 BOLA(I2)"
  ```

**发现记录**：I2（云手机 Create/Update 绑定 `proxy_id` 无属主/存在性校验的 BOLA/IDOR）由本次补测暴露，已在 `modules/proxy/proxy.go` 新增 `GetByID` 门面并在 `modules/phone/internal/service.go` 的 `Create`/`Update` 接入校验修复；按 `fix-and-record-bugs-during-testing` 记录（补测发现生产 bug → 直接修 + 逐条记录供复盘）。
---

### Task 6: 代理探测 SSRF/内网拦截 HTTP 回归（S2）

**类型**：回归防护（`modules/proxy/internal/prober.go` 已加 `resolvePublicHostPort`/`isDisallowedIP`，拨号前拒绝内网/环回/链路本地/CGNAT，测试应立即通过）

**Files:**
- Test: `/home/root/workspace005/gloryphone-code/backend/apptest/proxy_test.go`（追加一个测试函数于文件末尾，第 118 行之后）
- 只读参考（不改）：`modules/proxy/internal/api.go`（`ProbeProxy`，L169-185，路由体 `ProxyProbeRequest`→`ProxyService.Probe`→`framework.OKWithData`）、`modules/proxy/internal/service.go`（`Probe`，L139-161，探测失败返回 `&ProbeOutcome{Status: StatusFail, Message: err.Error()}, nil`，非接口错误）、`modules/proxy/internal/prober.go`（`resolvePublicHostPort` L106-117 / `isDisallowedIP` L138-152 / `reservedCIDRs` L121-134）、`modules/proxy/internal/model.go`（`ProxyProbeRequest` L56-61 字段 `host`/`port`；`ProbeOutcome` L64-75 字段 `status`/`message`）、`modules/proxy/internal/module.go`（L32 `g.POST("/probe", ProbeProxy)`，挂在 `/proxy` 组、走前台 `user.AuthMiddleware`）

**Interfaces:**
- Consumes（apptest 既有 helper，勿重定义）：`setupRouter() *gin.Engine`、`registerUser(t,r,phone string) string`、`doJSON(r,method,path,token,body) *httptest.ResponseRecorder`、`decode(t,w) framework.Response`
- Produces（新增测试函数）：`func TestProxyProbeRejectsPrivateHTTP(t *testing.T)`

关键事实（据 Read 确认，据此写断言）：
- 路由：`POST /api/v1/proxy/probe`，前台身份域，需 `registerUser` 拿的前台 token（无 token → 401）。
- 请求体字段 JSON tag：`host`（必填）、`port`（必填）、`username`、`password`。
- 拦截返回形态：`resolvePublicHostPort` 对内网/环回/链路本地/CGNAT/未指定地址返回 error → `Probe` 捕获后返回 `ProbeOutcome{Status:"fail", Message: err.Error()}` 且 err=nil。因此 **HTTP 状态码是 200，业务 `Code==0`**，`Data` 是 map，`Data["status"]=="fail"` 且 `Data["message"]` 非空。绝不会返回真实公网连通结果（不真拨号）。
- 覆盖地址均落在 `isDisallowedIP`：`127.0.0.1`（环回）、`10.0.0.1`（私网）、`169.254.169.254`（链路本地/云元数据）、`100.64.0.1`（CGNAT，`reservedCIDRs` 命中）。
- 不落库（`Probe` 不写 DB），无需 `t.Cleanup`。
- 网段边界细节由已有 `modules/proxy/internal/prober_ssrf_test.go`（`TestResolvePublicHostPort_*`/`TestIsDisallowedIP`）覆盖；本任务只验 apptest 层 HTTP 表现。

- [ ] **Step 1: 写失败测试** —— 在 `apptest/proxy_test.go` 末尾追加：

```go
// S2 回归：即时探测 /proxy/probe 必须拦截内网/环回/链路本地/CGNAT 地址，
// 防被当作内网端口扫描/云元数据 SSRF 原语。拦截命中拨号前的 resolvePublicHostPort，
// Probe 把 error 收敛成 ProbeOutcome{status:"fail", message:...}（非接口错误），
// 故 HTTP 200 + 业务 Code=0，但 data.status=="fail" 且 data.message 非空。
// 只验拦截路径，不传公网地址（避免真实拨号）。
func TestProxyProbeRejectsPrivateHTTP(t *testing.T) {
	r := setupRouter()
	token := registerUser(t, r, "13900020006")

	// 未登录不可访问该前台接口
	assert.Equal(t, http.StatusUnauthorized,
		doJSON(r, "POST", "/api/v1/proxy/probe", "", map[string]interface{}{
			"host": "203.0.113.7", "port": 1080,
		}).Code)

	for _, host := range []string{
		"127.0.0.1",       // 环回
		"10.0.0.1",        // 私网
		"169.254.169.254", // 链路本地 / 云元数据
		"100.64.0.1",      // CGNAT（RFC6598）
	} {
		w := doJSON(r, "POST", "/api/v1/proxy/probe", token, map[string]interface{}{
			"host": host, "port": 6379,
		})
		// 拦截被收敛为“探测失败”结果：HTTP 200 + 业务成功码，但内容判失败。
		require.Equal(t, http.StatusOK, w.Code, "host=%s body=%s", host, w.Body.String())
		resp := decode(t, w)
		require.Equal(t, 0, resp.Code, "host=%s 业务码应为 0（探测结果而非接口错误）", host)
		data, ok := resp.Data.(map[string]interface{})
		require.True(t, ok, "host=%s data 应为对象", host)
		assert.Equal(t, "fail", data["status"], "host=%s 应判为探测失败（被内网拦截）", host)
		assert.NotEmpty(t, data["message"], "host=%s 失败应带拦截原因", host)
		// 内网拦截发生在拨号前，绝不该返回出口 IP
		assert.Empty(t, data["egress_ip"], "host=%s 被拦截不应有出口 IP", host)
	}
}
```

- [ ] **Step 2: 运行验证失败** —— Run: `go test ./apptest/ -run TestProxyProbeRejectsPrivateHTTP -v`；Expected: 若 S2 拦截未生效（假设回退到未加 `resolvePublicHostPort` 的旧代码），则拨号会真实尝试连本机 6379 等端口，`data["status"]` 可能非 `"fail"`（连上时为 `ok`）或 `egress_ip` 非空、或断言 `NotEmpty(message)` 失败 → 测试 FAIL。当前已修复代码下本步用于确认断言绑定正确（回归任务下预期直接 PASS，见 Step 4）。

- [ ] **Step 3: 实现/修复（回归任务 → 确认已有实现满足）** —— 无需改生产码。核对要点：
  - `prober.go` L58 `Probe` 在构造 SOCKS5 拨号器前先调 `resolvePublicHostPort(ctx, p.Host, p.Port)`，其 L111-115 对所有解析出的 IP 逐个 `isDisallowedIP`，命中即 `return "", error`。
  - `isDisallowedIP`（L138-152）覆盖 `IsLoopback/IsPrivate/IsLinkLocalUnicast/IsLinkLocalMulticast/IsMulticast/IsUnspecified` + `reservedCIDRs`（含 `100.64.0.0/10` CGNAT）→ 用例四个地址全命中。
  - `service.go` L146-149 `Probe` 把该 error 收敛为 `&ProbeOutcome{Status: StatusFail, Message: err.Error()}, nil`，`api.go` L179-184 走 `framework.OKWithData` → HTTP 200、`Code=0`、`Data.status=="fail"`。断言与此形态一致。

- [ ] **Step 4: 运行验证通过** —— Run: `go test ./apptest/ -run TestProxyProbeRejectsPrivateHTTP -v`；Expected: PASS（四个内网/环回/元数据/CGNAT 地址均返回 `status=fail` + 非空 `message`、空 `egress_ip`；无 token 请求 401）。

- [ ] **Step 5: 提交** —— `git add backend/apptest/proxy_test.go` 后提交，message：`test(proxy): 补代理探测内网/元数据/CGNAT SSRF 拦截 HTTP 回归(S2)`
---

### Task 7: 订单支付并发/重复回调幂等（B1/B2）

**类型**：回归防护（bizorder 的 `markPaid` 状态守卫 CAS 已加：`WHERE id=? AND status<>paid`，`pay`/`MarkPaid` 均 CAS 先行、并发/重复仅一个赢家履约。测试应立即通过）

**Files:**
- Test（新建）：`/home/root/workspace005/gloryphone-code/backend/apptest/billing_pay_test.go`
- 参考只读（勿改）：`modules/billing/internal/module.go`（路由 L34-89）、`bizorder_service.go`（`CreateOrder` L113-206 恒调 `pay`；`pay` L244-274 与 `MarkPaid` L277-310 的 CAS+事务；`MarkPaid` 对已付订单 L285-287 提前返回）、`bizorder_repository.go`（`markPaid` CAS L68-72）、`bizorder_model.go`（表 `billing_biz_orders`、状态枚举 `unpaid/paid`、`BizSeatNew="seat_new"`、`PayBalance="balance"`）、`model.go`（`billing_ledger_entries`：`type="purchase"`、`order_id`）、`license_model.go`（`billing_license_units`：`kind="seat"`、`source="order"`、`source_ref`）

**Interfaces:**
- Consumes（既有 apptest helper，勿重定义）：`setupRouter() *gin.Engine`、`doJSON(r, method, path, token string, body interface{}) *httptest.ResponseRecorder`、`decode(t, w) framework.Response`、`registerUser(t, r, phone string) string`、`adminToken(t, r) string`；全局 `framework.DB *gorm.DB`、`framework.Response{Code int, Message string, Data interface{}}`
- HTTP 契约：`POST /api/v1/billing/topup`（user token，body `{"amount_cents":N}`，桩直充）、`POST /api/v1/billing/orders`（user token，body `BizOrderCreate`；余额购买即时扣款+履约+置已付）、`POST /api/v1/admin/billing/biz-orders/:id/mark-paid`（admin token，权限 `billing:manage`）
- Produces（新增测试函数）：`TestBizOrderMarkPaidIdempotentHTTP`

- [ ] **Step 1: 写失败测试** —— 新建 `/home/root/workspace005/gloryphone-code/backend/apptest/billing_pay_test.go`：

```go
package apptest

import (
	"fmt"
	"net/http"
	"sync"
	"testing"

	"manager-backend/framework"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// 回归防护（审查报告 B1/B2）：订单支付的 markPaid 以 WHERE status<>paid 的 CAS 守卫置已付，
// pay/MarkPaid 均 CAS 先行——并发/重复回调下仅一个赢家履约，杜绝重复扣款 + 重复履约。
//
// 场景：前台用户先充值 → 创建 seat_new 余额订单（下单即扣款+履约一次并置已付）→
// 并发 + 重复多次调用后台 mark-paid（对已付订单幂等）→ 用 framework.DB 断言：
//   - billing_ledger_entries 中该订单 purchase 类流水仅 1 条（余额只扣一次，B1）
//   - billing_license_units 中该订单只生成 1 个席位授权单元（履约只发生一次，B2）
//   - billing_biz_orders 该订单 total_cents 只被扣一次对应的余额（用充值后余额差断言）。
// 唯一手机号 + t.Cleanup 精确清理自造数据，绝不截断共享表。
func TestBizOrderMarkPaidIdempotentHTTP(t *testing.T) {
	r := setupRouter()

	const phone = "13977700701" // 唯一手机号，避免与其他用例冲突
	token := registerUser(t, r, phone)
	adminTok := adminToken(t, r)

	// 由手机号解析该用户的自增 ID（apptest 不能 import user internal，走 framework.DB 原生查 users 表）。
	var userID uint
	require.NoError(t,
		framework.DB.Table("users").Where("phone = ?", phone).Select("id").Scan(&userID).Error)
	require.NotZero(t, userID, "应能查到刚注册用户的 ID")

	// 只清理本用例造的行（按 user_id / phone 精确删），不 CleanTable 共享表。
	t.Cleanup(func() {
		framework.DB.Exec("DELETE FROM billing_biz_order_items WHERE order_id IN (SELECT id FROM billing_biz_orders WHERE user_id = ?)", userID)
		framework.DB.Exec("DELETE FROM billing_biz_orders WHERE user_id = ?", userID)
		framework.DB.Exec("DELETE FROM billing_ledger_entries WHERE user_id = ?", userID)
		framework.DB.Exec("DELETE FROM billing_license_units WHERE user_id = ?", userID)
		framework.DB.Exec("DELETE FROM billing_accounts WHERE user_id = ?", userID)
		framework.DB.Exec("DELETE FROM users WHERE id = ?", userID)
	})

	// 1) 充值：桩直充，余额足够买 1 席位（seed 单价 3000 分/台/月，充 100000 分绰绰有余）。
	wTopup := doJSON(r, "POST", "/api/v1/billing/topup", token, map[string]interface{}{
		"amount_cents": 100000,
	})
	require.Equal(t, http.StatusOK, wTopup.Code, "充值失败: %s", wTopup.Body.String())

	// 记录充值后、下单前的余额（用于断言只扣一次）。
	var balBefore int64
	require.NoError(t,
		framework.DB.Table("billing_accounts").Where("user_id = ?", userID).Select("balance_cents").Scan(&balBefore).Error)
	require.Equal(t, int64(100000), balBefore)

	// 2) 创建 seat_new 余额订单：CreateOrder 恒调 pay()，余额购买即时扣款 + 履约一次并置已付。
	wCreate := doJSON(r, "POST", "/api/v1/billing/orders", token, map[string]interface{}{
		"biz_type":       "seat_new",
		"quantity":       1,
		"duration_value": 1,
		"pay_method":     "balance",
	})
	require.Equal(t, http.StatusOK, wCreate.Code, "下单失败: %s", wCreate.Body.String())
	createData := decode(t, wCreate).Data.(map[string]interface{})
	order := createData["order"].(map[string]interface{})
	orderID := int(order["id"].(float64))
	require.NotZero(t, orderID)
	assert.Equal(t, "paid", order["status"], "余额购买下单即置已付")
	totalCents := int64(order["total_cents"].(float64))
	require.Equal(t, int64(3000), totalCents, "seed: 席位 3000 分/台 × 1 台 × 1 月")

	// 履约一次后的基线：purchase 流水 1 条、席位单元 1 个、余额已扣 total。
	assert.Equal(t, int64(1), countPurchaseLedger(t, userID, orderID), "下单即扣款一次")
	assert.Equal(t, int64(1), countSeatUnits(t, userID, orderID), "下单即履约生成 1 个席位单元")

	// 3) 并发 + 重复多次 mark-paid（订单已 paid，CAS 命中 0 行、提前返回，均应幂等）。
	//    sqlite 下事务串行，但幂等语义仍必须成立。
	const workers = 8
	var wg sync.WaitGroup
	codes := make([]int, workers)
	for i := 0; i < workers; i++ {
		wg.Add(1)
		go func(idx int) {
			defer wg.Done()
			w := doJSON(r, "POST",
				fmt.Sprintf("/api/v1/admin/billing/biz-orders/%d/mark-paid", orderID), adminTok, nil)
			codes[idx] = w.Code
		}(i)
	}
	wg.Wait()
	for i, code := range codes {
		assert.Equal(t, http.StatusOK, code, "第 %d 次 mark-paid 应幂等成功返回 200", i)
	}

	// 4) 断言：重复回调后无重复履约、无重复扣款。
	assert.Equal(t, int64(1), countPurchaseLedger(t, userID, orderID),
		"重复 mark-paid 后 purchase 流水仍应仅 1 条（B1 不重复扣款）")
	assert.Equal(t, int64(1), countSeatUnits(t, userID, orderID),
		"重复 mark-paid 后席位单元仍应仅 1 个（B2 不重复履约）")

	var balAfter int64
	require.NoError(t,
		framework.DB.Table("billing_accounts").Where("user_id = ?", userID).Select("balance_cents").Scan(&balAfter).Error)
	assert.Equal(t, balBefore-totalCents, balAfter, "余额只应被扣一次 total_cents")
}

// countPurchaseLedger 统计某订单在统一流水中的 purchase 类记录数（每次履约扣款写一条）。
func countPurchaseLedger(t *testing.T, userID uint, orderID int) int64 {
	t.Helper()
	var n int64
	require.NoError(t, framework.DB.Table("billing_ledger_entries").
		Where("user_id = ? AND type = ? AND order_id = ?", userID, "purchase", orderID).
		Count(&n).Error)
	return n
}

// countSeatUnits 统计某订单履约生成的席位授权单元数（source_ref 由履约固化为 biz_order:<id>）。
func countSeatUnits(t *testing.T, userID uint, orderID int) int64 {
	t.Helper()
	var n int64
	require.NoError(t, framework.DB.Table("billing_license_units").
		Where("user_id = ? AND kind = ? AND source = ? AND source_ref = ?",
			userID, "seat", "order", fmt.Sprintf("biz_order:%d", orderID)).
		Count(&n).Error)
	return n
}
```

- [ ] **Step 2: 运行验证失败** —— Run: `cd /home/root/workspace005/gloryphone-code/backend && go test ./apptest/ -run TestBizOrderMarkPaidIdempotentHTTP -v`；Expected: 回归防护任务，若 CAS 守卫在位则**本步应直接 PASS**。若此步 FAIL（如 purchase 流水或席位单元 >1、余额被多扣），则说明 `markPaid` 的 CAS 守卫或 `MarkPaid` 对已付订单的提前返回被破坏——即真实回归缺陷，进入 Step 3 修复。

- [ ] **Step 3: 确认已有实现满足** —— Read 复核以下要点，确认无需改生产码：
  - `bizorder_repository.go` L68-72 `markPaid`：`r.db.Model(&BizOrder{}).Where("id = ? AND status <> ?", id, BizOrderPaid).Update("status", BizOrderPaid)`，返回 `RowsAffected`——已付订单命中 0 行。
  - `bizorder_service.go` L277-310 `MarkPaid`：先 `get`，`o.Status == BizOrderPaid` 时 L285-287 直接返回（本用例下单即已 paid，8 次回调全部走这条提前返回，根本不进事务、不履约）；即便进事务，CAS `rows==0 → errOrderAlreadyPaid` 被 `errors.Is` 吞为幂等成功、不履约。
  - `bizorder_service.go` L244-274 `pay`：CAS + `chargeIfBalance` + `fulfillOrder` 收进单事务（下单时仅这一次真实扣款+履约）。
  - 结论：CAS 守卫已在位，本任务为回归防护，Step 2 应已 PASS，无需修改任何生产文件。

- [ ] **Step 4: 运行验证通过** —— Run: `cd /home/root/workspace005/gloryphone-code/backend && go test ./apptest/ -run TestBizOrderMarkPaidIdempotentHTTP -v && go vet ./apptest/`；Expected: PASS（`ok manager-backend/apptest`，vet 无输出）。

- [ ] **Step 5: 提交** —— 
```
git add backend/apptest/billing_pay_test.go
git commit -m "test(billing): 订单并发/重复 mark-paid 幂等回归（B1/B2 不重复扣款+履约）"
```
---

### Task 8: 余额支付"扣款成功履约失败"整单回滚（B3）

**类型**：回归防护（履约与扣款、CAS 置已付已收进 `pay()` 的单个 `s.db.Transaction`；本测试应立即通过。若日后有人把 `fulfillOrder` 移出事务，本测试会失败以暴露"扣了钱没发货"的回归）。

**Files:**
- Test: `modules/billing/internal/bizorder_service_test.go`（追加于文件末尾，第 359 行之后；`package billing` internal 单测）

**Interfaces:**
- Consumes（均为 billing 包内既有符号）：
  - `func (s *walletServiceImpl) TopUp(userID int, cents int64, reason, operator string) error`（`WalletService.TopUp`）
  - `func (s *walletServiceImpl) BalanceCents(userID int) (int64, error)`（`WalletService.BalanceCents`）
  - `func newBizOrderRepository(db *gorm.DB) bizOrderRepository`；其 `create(o *BizOrder, items []BizOrderItem) error` 与 `get(id int) (*BizOrder, []BizOrderItem, error)`
  - `func (s *bizOrderServiceImpl) PayOrder(userID, id int) (*BizOrderResult, error)`（`BizOrderService.PayOrder`）
  - 常量：`BizSeatNew`、`BizOrderUnpaid`、`PayBalance`、`KindSeat`、`DiscountBpsFull`、`LedgerPurchase`；模型 `BizOrder`/`BizOrderItem`/`LicenseUnit`/`LedgerEntry`/`Account`
  - `framework.DB (*gorm.DB)`
- Produces：测试函数 `TestPay_FulfillFailureRollsBackChargeAndOrder_B3`

关键事实（Read 已核对）：`pay()`（`bizorder_service.go:244-274`）在单个 `s.db.Transaction` 内依次做 `markPaid`(CAS) → `chargeIfBalance`(余额扣款+写 `purchase` 流水) → `fulfillOrder`；`fulfillOrder`(`:313`) 对 `BizSeatNew` 调 `s.fulfill.FulfillNew(userID, KindSeat, item.Quantity, item.DurationValue, SourceOrder, "biz_order:"+id)`，而 `FulfillNew`(`fulfill_service.go:47-49`) 在 `durationValue < 1` 时 `return apperr.Validation("时长必须≥1")`。因此构造一张 `DurationValue: 0` 的 seat 订单并直调 `PayOrder`，扣款会成功、履约必失败 → 整个事务回滚。绕开 `CreateOrder`（其 `QuoteKindDuration` 会在下单前就拦掉非法时长），用 `repo.create` 直接落一张合法 `TotalCents` 的 unpaid 订单是唯一能让"扣款成功但履约失败"发生的路径。扣款流水由 `chargeIfBalance` 写成 `type=purchase, order_id=o.ID`（`bizorder_service.go:239`）；履约的授权单元 `source_ref` 为 `"biz_order:"+strconv.Itoa(id)`（`:314`）。

- [ ] **Step 1: 写测试（追加到 `bizorder_service_test.go` 末尾）** ——

```go
// B3 回归：余额充足、扣款成功但履约失败（非法 duration_value=0 触发 FulfillNew 报错）时，
// CAS 置已付 + 扣款 + 履约同处 pay() 的单事务，任一步失败整单回滚：
// 订单仍 unpaid、无授权单元、余额未减、无 purchase 扣款流水。
// 直接 repo.create 落单以绕开 CreateOrder 的下单前校验（否则非法时长在报价阶段就被拦下，
// 到不了"已扣款"这一步）。按 user_id 精准清理自造行，勿 CleanTable 截断共享表。
func TestPay_FulfillFailureRollsBackChargeAndOrder_B3(t *testing.T) {
	uid := 950030
	t.Cleanup(func() {
		framework.DB.Where("user_id = ?", uid).Delete(&BizOrder{})
		framework.DB.Where("user_id = ?", uid).Delete(&LicenseUnit{})
		framework.DB.Where("user_id = ?", uid).Delete(&LedgerEntry{})
		framework.DB.Where("user_id = ?", uid).Delete(&Account{})
	})

	// 余额充足，确保扣款一定成功（失败点必须落在履约）。
	require.NoError(t, WalletService.TopUp(uid, 1000000, "test", "test"))
	balBefore, err := WalletService.BalanceCents(uid)
	require.NoError(t, err)

	// 直接落一张 unpaid seat 订单，订单项时长非法(0) → FulfillNew 必返回 422。
	repo := newBizOrderRepository(framework.DB)
	o := &BizOrder{
		UserID:     uint(uid),
		BizType:    BizSeatNew,
		Status:     BizOrderUnpaid,
		TotalCents: 3000,
		PayMethod:  PayBalance,
	}
	item := BizOrderItem{
		TargetKind: KindSeat, Quantity: 1, DurationValue: 0,
		UnitPriceCents: 3000, AmountCents: 3000,
		QtyDiscountBps: DiscountBpsFull, DurationDiscountBps: DiscountBpsFull,
		DurationUnit: "month",
	}
	require.NoError(t, repo.create(o, []BizOrderItem{item}))

	// 支付：扣款成功、履约失败 → 整单事务回滚，PayOrder 返回错误。
	_, err = BizOrderService.PayOrder(uid, int(o.ID))
	require.Error(t, err, "履约失败应导致整单支付失败")

	// 断言 1：订单仍 unpaid、未标记支付时间。
	reloaded, _, err := repo.get(int(o.ID))
	require.NoError(t, err)
	assert.Equal(t, BizOrderUnpaid, reloaded.Status, "订单应回滚为未支付")
	assert.Nil(t, reloaded.PaidAt, "回滚后不应有支付时间")

	// 断言 2：billing_license_units 无该订单履约出的行。
	var unitCount int64
	require.NoError(t, framework.DB.Model(&LicenseUnit{}).
		Where("user_id = ? AND source_ref = ?", uid, "biz_order:"+strconv.Itoa(int(o.ID))).
		Count(&unitCount).Error)
	assert.Equal(t, int64(0), unitCount, "履约回滚后不应残留授权单元")

	// 断言 3：余额未减（扣款随事务回滚）。
	balAfter, err := WalletService.BalanceCents(uid)
	require.NoError(t, err)
	assert.Equal(t, balBefore, balAfter, "扣款应随事务回滚，余额不变")

	// 断言 4：billing_ledger_entries 无该订单的 purchase 扣款条目。
	var chargeCount int64
	require.NoError(t, framework.DB.Model(&LedgerEntry{}).
		Where("user_id = ? AND type = ? AND order_id = ?", uid, LedgerPurchase, o.ID).
		Count(&chargeCount).Error)
	assert.Equal(t, int64(0), chargeCount, "扣款流水应随事务回滚")
}
```

> 注：文件顶部 import 已含 `strconv`? 否——现有 `bizorder_service_test.go` 只 import 了 `encoding/json`/`testing`/`time`/`framework`/`assert`/`require`。追加本测试后需在 import 块补一行 `"strconv"`（`framework` 与 `assert`/`require` 已在；`time`/`json` 保留给同文件其它用例）。

- [ ] **Step 2: 运行验证（先确认能编译并跑起来）** —— Run：
  `go test ./modules/billing/internal/ -run TestPay_FulfillFailureRollsBackChargeAndOrder_B3 -v`
  Expected：因这是回归防护、修复已在（`pay()` 已把 `chargeIfBalance`+`fulfillOrder` 收进单事务），本测试**直接 PASS**。要人工确认"没有修复会红"，可临时在 `pay()` 里把 `return stx.fulfillOrder(...)` 改成先 `if err := stx.chargeIfBalance(...); err != nil { return err }; _ = stx.fulfillOrder(...); return nil`（吞掉履约错误）——此时断言 1（订单变 paid）、断言 3/4（余额减、出现 purchase 流水）会 FAIL，暴露"扣了钱没发货"。验证完务必还原。

- [ ] **Step 3: 确认已有实现满足（回归任务，无需改生产码）** —— Read `modules/billing/internal/bizorder_service.go:244-274`（`pay`）核对三步均在 `s.db.Transaction(func(tx){...})` 内、任一步 `return err` 即触发 GORM 回滚；Read `:236-242`（`chargeIfBalance`：`PayBalance && BizType!=recharge && TotalCents>0` 才扣款并写 `LedgerPurchase` 流水）；Read `fulfill_service.go:40-49`（`FulfillNew` 在 `durationValue < 1` 返回 `apperr.Validation`）。结论：事务边界正确，测试所依赖的失败注入点与回滚语义均已具备，**无需修改任何生产文件**。

- [ ] **Step 4: 运行验证通过** —— Run：
  `go test ./modules/billing/internal/ -run TestPay_FulfillFailureRollsBackChargeAndOrder_B3 -v`；
  再跑同包全量确认无回归：`go test ./modules/billing/internal/`
  Expected：均 PASS（本用例 `--- PASS: TestPay_FulfillFailureRollsBackChargeAndOrder_B3`）。

- [ ] **Step 5: 提交** ——
  `git add backend/modules/billing/internal/bizorder_service_test.go`
  commit message：`test(billing): 补 B3 回归——余额扣款成功但履约失败时整单回滚`
---

### Task 9: 试用超领防护 + 唯一约束（B5/X4）

**类型**：回归防护（试用限领守卫 COUNT-then-INSERT + `billing_trial_policies.code` 唯一索引已存在于生产码，测试应立即通过）

**Files:**
- Create/Test: `/home/root/workspace005/gloryphone-code/backend/apptest/billing_trial_test.go`（新建，HTTP 集成测试，package apptest）

**Interfaces:**
- Consumes（既有 apptest helper，勿重定义）：
  - `setupRouter() *gin.Engine`
  - `doJSON(r *gin.Engine, method, path, token string, body interface{}) *httptest.ResponseRecorder`
  - `decode(t *testing.T, w *httptest.ResponseRecorder) framework.Response`
  - `adminToken(t *testing.T, r *gin.Engine) string`（内置超管 admin/admin123，scope=staff）
  - `framework.DB *gorm.DB`（原生 SQL 造数/断言；表名 `billing_trial_policies` / `billing_trial_claims` / `billing_license_units` / `users`）
- 被验证的既有路由/生产实现：
  - `POST /api/v1/admin/billing/trials`（handler `AdminCreateTrialPolicy`，中间件 `staff.PermissionMiddleware("billing:manage")`；请求体 `TrialPolicyCreate`：`code`/`name`/`items[]{subject,quantity,expire_days}`/`per_user_limit`/`allow_new_user`/`invite_code`/`enabled`）
  - `POST /api/v1/billing/trials/:code/claim`（handler `ClaimTrial`，中间件 `user.AuthMiddleware()`；请求体 `ClaimRequest`：`invite_code`）
  - `trialServiceImpl.ClaimTrial` → `gormTrialRepository.claim`（事务内 COUNT `billing_trial_claims` ≥ `per_user_limit` 即 `apperr.Conflict("已达领取上限")`→409）
  - `trialServiceImpl.CreatePolicy`（同 `code` 已存在 → `apperr.Conflict("策略编码已存在")`→409；`TrialPolicy.Code` 带 `uniqueIndex:idx_billing_trial_code`）
- Produces（本任务新增测试函数）：
  - `TestTrialClaimOverLimitAndUniqueCodeHTTP(t *testing.T)`
  - 文件内私有辅助 `registerUserWithID(t, r, phone) (token string, uid int)`（`registerUser` 只返回 token，这里需要 uid 做 DB 断言，故本文件内另写；名字不与既有 helper 冲突）

- [ ] **Step 1: 写失败测试** —— 新建 `/home/root/workspace005/gloryphone-code/backend/apptest/billing_trial_test.go`：

```go
package apptest

import (
	"fmt"
	"net/http"
	"testing"

	"manager-backend/framework"

	"github.com/gin-gonic/gin"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// registerUserWithID 注册前台用户并返回 (token, userID)。
// registerUser helper 只回 token，这里的 DB 断言需要 user_id，故本文件内另写一个。
func registerUserWithID(t *testing.T, r *gin.Engine, phone string) (string, int) {
	t.Helper()
	w := doJSON(r, "POST", "/api/v1/user/auth/register", "", map[string]string{
		"phone": phone, "password": "pass123",
	})
	require.Equal(t, http.StatusOK, w.Code, "注册失败: %s", w.Body.String())
	data := decode(t, w).Data.(map[string]interface{})
	return data["token"].(string), int(data["id"].(float64))
}

// TestTrialClaimOverLimitAndUniqueCodeHTTP 覆盖 B5/X4：
//   - per_user_limit=1 的试用策略，前台首领 200、再领 409（守卫式限领，不超领）；
//   - DB 侧断言 billing_trial_claims 恰 1 条、billing_license_units 恰生成 qty 条 active seat；
//   - 后台重复建同 code 策略 → 409（code 唯一约束）。
func TestTrialClaimOverLimitAndUniqueCodeHTTP(t *testing.T) {
	r := setupRouter()

	// 唯一命名，避免污染共享表 / 与其它用例冲突。
	const code = "apptest-trial-b5x4"
	phone := "13900" + fmt.Sprintf("%06d", 900501)
	const seatQty = 2

	// 只清理本用例自造的行（绝不 CleanTable 截断共享表）。
	t.Cleanup(func() {
		framework.DB.Exec("DELETE FROM billing_license_units WHERE source_ref = ?", "trial:"+code)
		framework.DB.Exec("DELETE FROM billing_trial_grants WHERE policy_id IN (SELECT id FROM billing_trial_policies WHERE code = ?)", code)
		framework.DB.Exec("DELETE FROM billing_trial_claims WHERE policy_id IN (SELECT id FROM billing_trial_policies WHERE code = ?)", code)
		framework.DB.Exec("DELETE FROM billing_trial_policy_items WHERE policy_id IN (SELECT id FROM billing_trial_policies WHERE code = ?)", code)
		framework.DB.Exec("DELETE FROM billing_trial_policies WHERE code = ?", code)
		framework.DB.Exec("DELETE FROM billing_ledger_entries WHERE reason = ?", "trial:"+code)
		framework.DB.Exec("DELETE FROM users WHERE phone = ?", phone)
	})

	admin := adminToken(t, r)
	userTok, uid := registerUserWithID(t, r, phone)

	// 后台建策略：per_user_limit=1，发放 seatQty 个 seat（30 天到期），允许新用户领取。
	createBody := map[string]interface{}{
		"code":           code,
		"name":           "上线验收-新人试用",
		"per_user_limit": 1,
		"allow_new_user": true,
		"items": []map[string]interface{}{
			{"subject": "seat", "quantity": seatQty, "expire_days": 30},
		},
	}
	w := doJSON(r, "POST", "/api/v1/admin/billing/trials", admin, createBody)
	require.Equal(t, http.StatusOK, w.Code, "建策略应成功: %s", w.Body.String())
	require.Equal(t, 0, decode(t, w).Code)

	// 前台首次领取 → 200。
	w = doJSON(r, "POST", "/api/v1/billing/trials/"+code+"/claim", userTok, map[string]string{})
	require.Equal(t, http.StatusOK, w.Code, "首次领取应成功: %s", w.Body.String())
	require.Equal(t, 0, decode(t, w).Code)

	// 再次领取 → 409（已达领取上限），不超领。
	w = doJSON(r, "POST", "/api/v1/billing/trials/"+code+"/claim", userTok, map[string]string{})
	assert.Equal(t, http.StatusConflict, w.Code, "二次领取应被拒: %s", w.Body.String())

	// DB 断言：领取头恰 1 条。
	var claims int64
	require.NoError(t, framework.DB.
		Raw("SELECT COUNT(*) FROM billing_trial_claims WHERE policy_id IN (SELECT id FROM billing_trial_policies WHERE code = ?) AND user_id = ?", code, uid).
		Scan(&claims).Error)
	assert.Equal(t, int64(1), claims, "限领 1：只应有 1 条领取头")

	// DB 断言：仅生成 seatQty 条 active seat 授权单元（不超领）。
	var seats int64
	require.NoError(t, framework.DB.
		Raw("SELECT COUNT(*) FROM billing_license_units WHERE user_id = ? AND kind = ? AND status = ? AND source_ref = ?", uid, "seat", "active", "trial:"+code).
		Scan(&seats).Error)
	assert.Equal(t, int64(seatQty), seats, "应恰生成 %d 条 active seat 授权单元", seatQty)

	// 后台重复建同 code 策略 → 409（code 唯一约束 / 服务层冲突守卫）。
	w = doJSON(r, "POST", "/api/v1/admin/billing/trials", admin, createBody)
	assert.Equal(t, http.StatusConflict, w.Code, "重复 code 应冲突: %s", w.Body.String())
}
```

- [ ] **Step 2: 运行验证失败** —— Run: `go test ./apptest/ -run TestTrialClaimOverLimitAndUniqueCodeHTTP -v`；Expected：本任务为回归防护，若生产实现完整应直接 PASS。仅当实现有缺陷时才 FAIL——具体失败面为：二次领取未返回 409（`assert.Equal http.StatusConflict` 处失败，出现超领导致 `billing_trial_claims`>1 或 `billing_license_units` active seat>seatQty），或重复 code 建策略未返回 409（code 唯一约束缺失）。

- [ ] **Step 3: 确认已有实现满足** —— 无需改生产码。需 Read 核对的要点（均已确认存在）：
  - `modules/billing/internal/trial_repository.go` 的 `gormTrialRepository.claim`（约 152–186 行）：事务内先 `Count(&claimed)` `billing_trial_claims` 再判 `int(claimed) >= policy.PerUserLimit` → `apperr.Conflict("已达领取上限")`，随后才 `Create(&TrialClaim{...})` 与遍历发放；sqlite 串行化下该 COUNT-then-INSERT 守卫防超领（注释亦标注迁 MySQL/PG 需补 `(policy_id,user_id)` 唯一约束或行锁）。
  - `modules/billing/internal/trial_service.go` 的 `ClaimTrial`（约 218–223 行）在 service 层亦有同名 `claimed >= p.PerUserLimit` → `apperr.Conflict` 前置守卫；`CreatePolicy`（约 81–83 行）对已存在 `code` 返回 `apperr.Conflict("策略编码已存在")`。
  - `modules/billing/internal/trial_model.go`：`TrialPolicy.Code` 带 `uniqueIndex:idx_billing_trial_code`（第 7 行），`AllowNewUser` 走 `countPaidOrders==0` 放行新用户领取。
  - `modules/billing/internal/trial_repository.go` 的 `grantTrialItem`（约 189–214 行）：`seat` 按 `Quantity` 建 N 个 `LicenseUnit{Status: LicenseActive, Source: SourceTrial, SourceRef: "trial:"+code}`。
  - `apperr.Conflict` → HTTP 409 由 `framework.FailErr`（`framework/response.go` 第 64–67 行经 `apperr.HTTPStatus`）映射。

- [ ] **Step 4: 运行验证通过** —— Run: `go test ./apptest/ -run TestTrialClaimOverLimitAndUniqueCodeHTTP -v`；Expected：PASS（首领 200、再领 409、`billing_trial_claims`=1、active seat=2、重复 code=409）。附带全量回归：`go test ./apptest/`。

- [ ] **Step 5: 提交** —— 
  ```
  git add backend/apptest/billing_trial_test.go
  git commit -m "test(billing): 覆盖试用限领超领防护与策略 code 唯一约束(B5/X4)"
  ```
---

### Task 10: 到期读时过滤 + markExpired 状态翻转（X9 计费时间核心）

**类型**：回归防护（时间测法，传构造 `now`，不等真实时间）。`activeUnits` 的 `status=active AND expire_at>now` 读时过滤与 `markExpired` 的批量翻转均已实现，测试应立即通过。

> **接线核实结论（务必写进测评结论）**：全仓 grep `markExpired`/`MarkExpired` 仅命中 `license_repository.go` 的接口声明（第 19 行）与实现（第 99 行），**无任何路由 / OnStart 定时任务 / service 调用触发它**。即生产环境的到期语义完全由**读时过滤**兜底：`activeUnits(uid,kind,now)`（`license_repository.go:39`，条件 `status = active AND expire_at > now`）在每次 `Capacity`/`ReconcileSeats`/`ListActiveUnits` 读取时排除已过期单元，`Capacity` 内部即调它（`license_service.go:26`）。`markExpired(now)` 是一个尚未接线的物化清理入口（把 active 且 `expire_at<=now` 翻成 `expired` 并清 `current_instance_id`），本任务直调 repo 验证其行为，同时验证「即便不调 markExpired，读时也已过滤」这一现网真实兜底。

**Files:**
- Create: `modules/billing/internal/license_repository_test.go`（新建，`package billing`，internal 单测；同包已有 `main_test.go` 的 `TestMain` 装配真实 sqlite + `LicenseService`）
- Read（核对，不改）：`modules/billing/internal/license_repository.go`（`activeUnits` 第 39 行 / `markExpired` 第 99 行）、`license_service.go`（`Capacity` 第 25 行）、`license_model.go`（`LicenseUnit` 字段与 `KindSeat`/`LicenseActive`/`LicenseExpired`/`TableName`）、`entitlement_model.go`（`SourceOrder` 第 11 行）、`license_service_test.go`（现有风格）、`main_test.go`（TestMain 装配）

**Interfaces:**
- Consumes（既有，勿重定义）：
  - `LicenseService.repo.create([]LicenseUnit) error`
  - `LicenseService.repo.activeUnits(userID int, kind string, now time.Time) ([]LicenseUnit, error)`
  - `LicenseService.repo.markExpired(now time.Time) (int64, error)`
  - `LicenseService.Capacity(userID int, kind string) (int, error)`（内部用 `time.Now()`，不接受注入 now，用**远期到期**规避与 wall-clock 的偏差）
  - `framework.DB *gorm.DB`（本包内可用，用于按 `user_id` 清理自造行）
  - 常量：`KindSeat`、`LicenseActive`、`LicenseExpired`、`SourceOrder`；表名 `billing_license_units`
- Produces（新增测试函数）：
  - `TestActiveUnits_ExpiredFilteredAtReadTime`
  - `TestMarkExpired_FlipsExpiredAndClearsInstance`

> **陷阱规避**：现有 `license_service_test.go` 用了 `framework.CleanTable("billing_license_units")`，但按测评基建约定**绝不 CleanTable 截断共享表**；本任务改用**唯一 uid + `t.Cleanup` 里按 `user_id` DELETE** 只清自己造的行，且避开现有测试所用 uid `920001/920002`，与之并存不冲突。`markExpired(now)` 是全表操作，测试仅断言「自己那条唯一 uid 的过期行被翻转」，不对全表行数下断言，避免受同库其它测试残留影响。

- [ ] **Step 1: 写失败测试** —— 新建 `modules/billing/internal/license_repository_test.go`：

```go
package billing

import (
	"testing"
	"time"

	"manager-backend/framework"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// cleanupLicenseUnitsByUser 只删除本测试自造的行（按唯一 uid），
// 不截断共享表（RunSetup 未 seed 授权单元，但整库测试共用一个 DB，需保持隔离）。
// Task 11（同包 fulfill_service_test.go）复用此函数，签名统一为 int。
func cleanupLicenseUnitsByUser(t *testing.T, uid int) {
	t.Helper()
	require.NoError(t, framework.DB.
		Where("user_id = ?", uid).
		Delete(&LicenseUnit{}).Error)
}

// TestActiveUnits_ExpiredFilteredAtReadTime 验证到期在「读时」被过滤：
// 即便不调 markExpired，activeUnits/Capacity 也只返回 expire_at>now 的 active 单元。
func TestActiveUnits_ExpiredFilteredAtReadTime(t *testing.T) {
	const uid = 920910
	t.Cleanup(func() { cleanupLicenseUnitsByUser(t, uid) })

	now := time.Now()
	require.NoError(t, LicenseService.repo.create([]LicenseUnit{
		{UserID: uid, Kind: KindSeat, Status: LicenseActive, Source: SourceOrder, ExpireAt: now.Add(24 * time.Hour)},
		{UserID: uid, Kind: KindSeat, Status: LicenseActive, Source: SourceOrder, ExpireAt: now.Add(-1 * time.Second)}, // 刚过期
		{UserID: uid, Kind: KindSeat, Status: LicenseActive, Source: SourceOrder, ExpireAt: now.Add(30 * 24 * time.Hour)},
	}))

	// 传构造 now：只应返回 now+24h 与 now+30d 两条，now-1s 被 expire_at>now 排除。
	units, err := LicenseService.repo.activeUnits(uid, KindSeat, now)
	require.NoError(t, err)
	require.Len(t, units, 2, "expire_at<=now must be excluded at read time")
	for _, u := range units {
		assert.True(t, u.ExpireAt.After(now), "returned unit must not be expired: id=%d expireAt=%s", u.ID, u.ExpireAt)
	}

	// Capacity 走 activeUnits(time.Now())：两条远期单元（+24h/+30d）在真实 wall-clock 下仍未过期，
	// now-1s 那条早已过期，故 Capacity 同样只数到 2。
	capSeat, err := LicenseService.Capacity(uid, KindSeat)
	require.NoError(t, err)
	assert.Equal(t, 2, capSeat, "Capacity counts only non-expired active seats")
}

// TestMarkExpired_FlipsExpiredAndClearsInstance 验证 markExpired 的物化翻转语义：
// active 且 expire_at<=now 的单元被置为 expired 并清空 current_instance_id；
// 翻转后 activeUnits 结果不变（本就已被读时过滤）。
func TestMarkExpired_FlipsExpiredAndClearsInstance(t *testing.T) {
	const uid = 920911
	t.Cleanup(func() { cleanupLicenseUnitsByUser(t, uid) })

	now := time.Now()
	require.NoError(t, LicenseService.repo.create([]LicenseUnit{
		{UserID: uid, Kind: KindSeat, Status: LicenseActive, Source: SourceOrder, ExpireAt: now.Add(24 * time.Hour)},
		{UserID: uid, Kind: KindSeat, Status: LicenseActive, Source: SourceOrder, CurrentInstanceID: "cp-expired", ExpireAt: now.Add(-1 * time.Second)}, // 刚过期且占用中
		{UserID: uid, Kind: KindSeat, Status: LicenseActive, Source: SourceOrder, ExpireAt: now.Add(30 * 24 * time.Hour)},
	}))

	// 翻转前：读时已过滤，activeUnits 只见 2 条未过期。
	before, err := LicenseService.repo.activeUnits(uid, KindSeat, now)
	require.NoError(t, err)
	require.Len(t, before, 2)

	// markExpired 目前无路由/定时任务触发（全仓仅接口声明+实现），此处直调 repo 验证其行为。
	affected, err := LicenseService.repo.markExpired(now)
	require.NoError(t, err)
	// markExpired 是全表操作，其它测试残留可能一并被翻转，故只断言"至少翻了本用例这一条"，
	// 具体行的状态用下面按 uid 的精确查询来确证。
	assert.GreaterOrEqual(t, affected, int64(1), "the just-expired seat must be affected")

	// 精确断言：本用户仅那条 now-1s 单元被翻成 expired 且 current_instance_id 清空。
	var mine []LicenseUnit
	require.NoError(t, framework.DB.
		Where("user_id = ?", uid).
		Order("expire_at DESC").
		Find(&mine).Error)
	require.Len(t, mine, 3)

	var expiredCount, activeCount int
	for _, u := range mine {
		switch {
		case u.ExpireAt.After(now):
			assert.Equal(t, LicenseActive, u.Status, "future-dated unit stays active: id=%d", u.ID)
			activeCount++
		default:
			assert.Equal(t, LicenseExpired, u.Status, "past-dated unit flipped to expired: id=%d", u.ID)
			assert.Equal(t, "", u.CurrentInstanceID, "expired unit must release its instance: id=%d", u.ID)
			expiredCount++
		}
	}
	assert.Equal(t, 1, expiredCount, "exactly one seat was expired")
	assert.Equal(t, 2, activeCount, "two future seats untouched")

	// 翻转后再读 activeUnits：结果不变（读时过滤与物化翻转口径一致）。
	after, err := LicenseService.repo.activeUnits(uid, KindSeat, now)
	require.NoError(t, err)
	require.Len(t, after, 2, "activeUnits unchanged after markExpired")
}
```

- [ ] **Step 2: 运行验证失败** —— Run：`go test ./modules/billing/internal/ -run 'TestActiveUnits_ExpiredFilteredAtReadTime|TestMarkExpired_FlipsExpiredAndClearsInstance' -v`；Expected：本步在真实实现下应**直接编译通过并 PASS**（回归防护任务）。若此时出现 FAIL，只可能来自以下真实缺陷之一，即暴露 bug：
  - `activeUnits` 的 SQL 用了 `expire_at >= ?` 或漏了 `status = active` → `require.Len(t, units, 2)` 失败（返回 3 条）；
  - `markExpired` 的 `Where` 用 `expire_at < ?`（漏等号）或漏更新 `current_instance_id` → `expiredCount`/`CurrentInstanceID==""` 断言失败；
  - `Capacity` 未走 `activeUnits`（改数全部行）→ `assert.Equal(t, 2, capSeat)` 失败。
  （编译期若报 `LicenseService` 或 `repo` 未定义，说明 `main_test.go` 的 `Init` 装配被破坏，需先修装配。）

- [ ] **Step 3: 确认已有实现满足（回归任务）** —— Read 核对下列要点，确认无需改生产码：
  - `license_repository.go:39` `activeUnits`：`Where("user_id = ? AND kind = ? AND status = ? AND expire_at > ?", userID, kind, LicenseActive, now)` —— `expire_at > now`（严格大于）保证 `now-1s` 被排除、`now+24h/+30d` 保留，`status = LicenseActive` 保证只数 active。
  - `license_repository.go:99` `markExpired`：`Where("status = ? AND expire_at <= ?", LicenseActive, now).Updates({status: LicenseExpired, current_instance_id: ""})` —— `<= now` 命中刚过期单元，`Updates` 同时翻状态 + 清占用，`RowsAffected` 即受影响行数。
  - `license_service.go:26` `Capacity`：`s.repo.activeUnits(userID, kind, time.Now())` 后 `len(units)`，口径与读时过滤一致。
  - `license_model.go`：字段 JSON/列名 `user_id`/`status`/`current_instance_id`/`expire_at`，常量 `KindSeat="seat"`、`LicenseActive="active"`、`LicenseExpired="expired"`，`TableName()="billing_license_units"`；`entitlement_model.go:11` `SourceOrder="order"`。
  实现已满足，**不改任何生产文件**。

- [ ] **Step 4: 运行验证通过** —— Run：`go test ./modules/billing/internal/ -run 'TestActiveUnits_ExpiredFilteredAtReadTime|TestMarkExpired_FlipsExpiredAndClearsInstance' -v`；Expected：PASS（两个测试均 ok）。再跑 `go vet ./modules/billing/internal/` 无告警。

- [ ] **Step 5: 提交** —— 
  ```bash
  git add modules/billing/internal/license_repository_test.go
  git commit -m "test(billing): 覆盖授权单元到期读时过滤与 markExpired 状态翻转"
  ```
  （commit message 结尾不加署名。）
---

### Task 11: 续费从原到期叠加 + 永久单元（计费时间语义）

**类型**：回归防护（`FulfillRenew` 的"未过期从原到期起算、已过期从 now 起算"闭包与 `grantTrialItem` 的 `ExpireDays=0→100 年永久`语义均已实现，测试应立即通过）。测法：直接调 `internal` 的 service/包级函数，用构造的 `expire_at` 相对锚点与显式 `now` 做确定性断言。

**Files:**
- Test（追加）：`/home/root/workspace005/gloryphone-code/backend/modules/billing/internal/fulfill_service_test.go`
- 需先 Read 核对（只读，不改）：
  - `modules/billing/internal/fulfill_service.go`：`FulfillRenew`（第 76–105 行）内 `now := time.Now()`；闭包 `base := cur; if base.Before(now) { base = now }; return expireFrom(base, kind, durationValue)`；`expireFrom`（第 29–37 行）`KindSeat→base.AddDate(0,value,0)`（月）、`KindBootSlot→base.AddDate(0,0,value)`（天）。
  - `modules/billing/internal/license_repository.go`：`extendExpiry`（第 59–74 行）逐单元 `addFrom(u.ExpireAt)`；`getByIDs(userID,ids,kind)`、`create(units)`、`activeUnits(userID,kind,now)`。
  - `modules/billing/internal/trial_repository.go`：`grantTrialItem(tx *gorm.DB, userID int, it TrialPolicyItem, now time.Time, ref string) (int64, error)`（第 189–230 行）：seat/boot_slot 分支 `expire := now.AddDate(100,0,0); if it.ExpireDays>0 { expire = now.AddDate(0,0,it.ExpireDays) }`，直接 `tx.Create(&units)`。
  - `modules/billing/internal/license_model.go`：`LicenseUnit{UserID uint; Kind string; Status string; Source string; SourceRef string; ExpireAt time.Time}`，`TableName()="billing_license_units"`；`KindSeat="seat"`、`KindBootSlot="boot_slot"`、`LicenseActive="active"`。
  - `modules/billing/internal/trial_model.go`：`TrialPolicyItem{Subject string; Quantity int64; ExpireDays int}`。
  - `modules/billing/internal/main_test.go`：`TestMain` 已 `framework.SetupTestDB` + `billingModule.Init`，包级 `FulfillService`/`LicenseService` 已装配，`framework.DB` 可用。

**Interfaces:**
- Consumes（既有）：`FulfillService.FulfillRenew(userID int, kind string, unitIDs []uint, durationValue int) error`；`LicenseService.repo.create([]LicenseUnit) error`；`LicenseService.repo.getByIDs(userID int, ids []uint, kind string) ([]LicenseUnit, error)`；`LicenseService.repo.activeUnits(userID int, kind string, now time.Time) ([]LicenseUnit, error)`；包级 `grantTrialItem(tx *gorm.DB, userID int, it TrialPolicyItem, now time.Time, ref string) (int64, error)`；`framework.DB`。
- Produces（新增测试函数）：`TestFulfillRenew_StacksFromOriginalExpiryWhenActive`、`TestFulfillRenew_StacksFromNowWhenExpired`、`TestGrantTrialItem_ZeroExpireDaysIsPerpetual`。

- [ ] **Step 1: 写失败测试** —— 在 `modules/billing/internal/fulfill_service_test.go` 末尾追加以下代码（文件已 import `testing`/`time`/`manager-backend/framework`/`testify/assert`/`testify/require`，无需新增 import；不 `CleanTable`，按 `user_id` 用 `t.Cleanup` 清理自己造的行）：

```go
// 注：cleanupLicenseUnitsByUser(t *testing.T, uid int) 已由 Task 10 在同包
// （modules/billing/internal，license_repository_test.go）定义，本文件直接复用，勿重复定义。

// TestFulfillRenew_StacksFromOriginalExpiryWhenActive 未过期单元续费：新到期从原到期叠加（非从 now）。
func TestFulfillRenew_StacksFromOriginalExpiryWhenActive(t *testing.T) {
	uid := 930111
	cleanupLicenseUnitsByUser(t, uid)

	now := time.Now()
	orig := now.AddDate(0, 0, 10) // 原到期 = now+10 天（未过期）
	unit := LicenseUnit{
		UserID: uint(uid), Kind: KindSeat, Status: LicenseActive,
		Source: SourceOrder, SourceRef: "renew-active", ExpireAt: orig,
	}
	require.NoError(t, LicenseService.repo.create([]LicenseUnit{unit}))

	created, err := LicenseService.repo.activeUnits(uid, KindSeat, now)
	require.NoError(t, err)
	require.Len(t, created, 1)
	id := created[0].ID
	origSaved := created[0].ExpireAt // 以 DB 落库后的到期为锚点，规避时区/精度误差

	require.NoError(t, FulfillService.FulfillRenew(uid, KindSeat, []uint{id}, 1))

	after, err := LicenseService.repo.getByIDs(uid, []uint{id}, KindSeat)
	require.NoError(t, err)
	require.Len(t, after, 1)
	// 从原到期起算：新到期 ≈ 原到期 + 1 月，且明显晚于「从 now 起算」(now+1 月)。
	assert.WithinDuration(t, origSaved.AddDate(0, 1, 0), after[0].ExpireAt, 2*time.Hour)
	assert.True(t, after[0].ExpireAt.After(now.AddDate(0, 1, 0)),
		"未过期续费必须从原到期叠加，应晚于从 now 起算")
}

// TestFulfillRenew_StacksFromNowWhenExpired 已过期单元续费：从 now 起算（非从旧到期叠加）。
func TestFulfillRenew_StacksFromNowWhenExpired(t *testing.T) {
	uid := 930112
	cleanupLicenseUnitsByUser(t, uid)

	now := time.Now()
	stale := now.AddDate(0, 0, -5) // 原到期 = now-5 天（已过期）
	unit := LicenseUnit{
		UserID: uint(uid), Kind: KindSeat, Status: LicenseActive,
		Source: SourceOrder, SourceRef: "renew-expired", ExpireAt: stale,
	}
	require.NoError(t, LicenseService.repo.create([]LicenseUnit{unit}))

	// activeUnits 过滤 expire_at>now，已过期单元查不到，改用 getByIDs 全量取。
	all, err := LicenseService.repo.getByIDs(uid, nil, KindSeat)
	require.NoError(t, err)
	// getByIDs 的 IN ? 传 nil 可能匹配空集；改用按属主的一次性查回 ID。
	require.NoError(t, framework.DB.Where("user_id = ? AND kind = ?", uid, KindSeat).Find(&all).Error)
	require.Len(t, all, 1)
	id := all[0].ID

	renewAt := time.Now()
	require.NoError(t, FulfillService.FulfillRenew(uid, KindSeat, []uint{id}, 1))

	after, err := LicenseService.repo.getByIDs(uid, []uint{id}, KindSeat)
	require.NoError(t, err)
	require.Len(t, after, 1)
	// 从 now 起算：新到期 ≈ renewAt + 1 月；绝不是从旧到期(now-5d)叠加。
	assert.WithinDuration(t, renewAt.AddDate(0, 1, 0), after[0].ExpireAt, 24*time.Hour)
	assert.True(t, after[0].ExpireAt.After(stale.AddDate(0, 1, 0)).Equal(true) ||
		after[0].ExpireAt.After(stale.AddDate(0, 1, 0)),
		"已过期续费应从 now 起算，晚于从旧到期叠加")
}

// TestGrantTrialItem_ZeroExpireDaysIsPerpetual ExpireDays=0 视为永久：到期≈now+100 年，50 年后仍未过期。
func TestGrantTrialItem_ZeroExpireDaysIsPerpetual(t *testing.T) {
	uid := 930113
	cleanupLicenseUnitsByUser(t, uid)

	now := time.Now()
	it := TrialPolicyItem{Subject: KindSeat, Quantity: 1, ExpireDays: 0} // 0=永久
	balanceAfter, err := grantTrialItem(framework.DB, uid, it, now, "trial:perpetual")
	require.NoError(t, err)
	assert.Equal(t, int64(1), balanceAfter)

	units, err := LicenseService.repo.listByUserKind(uid, KindSeat)
	require.NoError(t, err)
	require.Len(t, units, 1)
	// 到期 ≈ now + 100 年。
	assert.WithinDuration(t, now.AddDate(100, 0, 0), units[0].ExpireAt, 48*time.Hour)

	// 50 年后仍属未过期可用池（activeUnits 过滤 expire_at>now）。
	future := now.AddDate(50, 0, 0)
	active, err := LicenseService.repo.activeUnits(uid, KindSeat, future)
	require.NoError(t, err)
	assert.Len(t, active, 1, "永久单元 50 年后仍应 active")
}
```

- [ ] **Step 2: 运行验证失败** —— Run: `go test ./modules/billing/internal/ -run 'TestFulfillRenew_StacksFromOriginalExpiryWhenActive|TestFulfillRenew_StacksFromNowWhenExpired|TestGrantTrialItem_ZeroExpireDaysIsPerpetual' -v`；Expected：编译失败 —— Step 1 中 `TestFulfillRenew_StacksFromNowWhenExpired` 的最后一条断言写成了 `.After(...).Equal(true)`（`time.Time.After` 返回 `bool`，`bool` 无 `.Equal` 方法），`go vet`/编译报 `after[0].ExpireAt.After(...).Equal undefined (type bool has no field or method Equal)`，测试无法运行（先失败）。

- [ ] **Step 3: 修复测试断言使其可编译（本任务为回归防护，生产码无需改动，仅修正测试自身的笔误断言）** —— 确认已 Read `fulfill_service.go`/`trial_repository.go`，其起算/永久语义均已正确实现，无需改生产码。将 Step 1 中 `TestFulfillRenew_StacksFromNowWhenExpired` 里那条病态断言替换为单行布尔断言：

```go
	// 从 now 起算：新到期 ≈ renewAt + 1 月；绝不是从旧到期(now-5d)叠加。
	assert.WithinDuration(t, renewAt.AddDate(0, 1, 0), after[0].ExpireAt, 24*time.Hour)
	assert.True(t, after[0].ExpireAt.After(stale.AddDate(0, 1, 0)),
		"已过期续费应从 now 起算，晚于从旧到期叠加")
```

- [ ] **Step 4: 运行验证通过** —— Run: `go test ./modules/billing/internal/ -run 'TestFulfillRenew_StacksFromOriginalExpiryWhenActive|TestFulfillRenew_StacksFromNowWhenExpired|TestGrantTrialItem_ZeroExpireDaysIsPerpetual' -v`；Expected: PASS（三个用例全绿；起算点两种路径、100 年永久语义均被覆盖）。随后 `go test ./modules/billing/internal/` 整包仍 PASS（未截断共享表，仅按 `user_id` 清理自造行）。

- [ ] **Step 5: 提交** —— Run: `git add modules/billing/internal/fulfill_service_test.go` 后 `git commit`，message：

```
test(billing): 覆盖续费从原到期叠加/已过期从 now 起算与永久单元语义
```
---

### Task 12: 到期链路 HTTP 端到端（X9：seed→backdate→容量下降）

**类型**：回归防护（时间测法）——验证 `activeUnits` 在读时用 `expire_at > now` 过滤，把 `expire_at` 回拨到过去即可让席位退出可用池，无需真等 30 天；测试应立即通过。

**Files:**
- Create/Test: `/home/root/workspace005/gloryphone-code/backend/apptest/billing_expiry_test.go`（新建）

**核对到的事实（写代码依据）:**
- 路由：`GET /api/v1/billing/overview`（`modules/billing/internal/module.go:46`，前缀 `router.Group("/billing")` 挂在 `/api/v1` 下，`user.AuthMiddleware()` 要前台令牌）。
- overview 响应（`billing2_api.go:59-64`）：`Data.seat = {"total": <int>, "used": <int>}`；`seat.total` 来自 `LicenseService.Capacity(uid, KindSeat)`。
- `Capacity`（`license_service.go:25`）→ `repo.activeUnits(userID, kind, time.Now())`；`activeUnits`（`license_repository.go:39-42`）过滤 `user_id = ? AND kind = ? AND status = 'active' AND expire_at > now`。把 `expire_at` 改到 `now-1s` 即令该行落选（status 仍 active，但 `expire_at > now` 为 false）。
- 表 `billing_license_units`（`license_model.go:24`），列（GORM snake_case）：`user_id`、`kind`、`status`、`current_instance_id`、`source`、`source_ref`、`order_item_id`、`expire_at`、`created_at`、`updated_at`。`kind` 取值 `seat`，`status` 取值 `active`，`source` 取值 `grant`（`SourceGrant`）。
- 用户表 `users`（`modules/user/internal/model.go:19`），列 `phone`、`id`；注册响应 `Data.id` 亦含 user id，但本测试从 `users` 表按唯一手机号回查 `user_id` 以拿到确切数值。
- helpers：`setupRouter()`、`doJSON`、`decode`、`registerUser(t,r,phone)`（`apptest/note_test.go:18`，密码固定 `pass123`，返回前台 token）、全局 `framework.DB *gorm.DB`（apptest TestMain 已装配）。

**Interfaces:**
- Consumes: `setupRouter() *gin.Engine`、`doJSON(r,method,path,token,body) *httptest.ResponseRecorder`、`decode(t,w) framework.Response`、`registerUser(t,r,phone) string`、`framework.DB (*gorm.DB)`
- Produces: 测试函数 `TestBillingSeatExpiryHTTP`（无新增生产码）

- [ ] **Step 1: 写失败测试** —— 新建 `/home/root/workspace005/gloryphone-code/backend/apptest/billing_expiry_test.go`：

```go
package apptest

import (
	"net/http"
	"testing"
	"time"

	"manager-backend/framework"

	"github.com/stretchr/testify/require"
)

// seatTotal 打 GET /billing/overview 取 Data.seat.total（未过期席位容量）。
func seatTotal(t *testing.T, token string) int {
	t.Helper()
	r := billingExpiryRouter
	w := doJSON(r, "GET", "/api/v1/billing/overview", token, nil)
	require.Equal(t, http.StatusOK, w.Code, "overview 请求失败: %s", w.Body.String())
	resp := decode(t, w)
	require.Equal(t, 0, resp.Code, "overview 业务码非 0: %s", w.Body.String())
	data, ok := resp.Data.(map[string]interface{})
	require.True(t, ok, "Data 不是对象: %v", resp.Data)
	seat, ok := data["seat"].(map[string]interface{})
	require.True(t, ok, "Data.seat 不是对象: %v", data["seat"])
	total, ok := seat["total"].(float64)
	require.True(t, ok, "seat.total 不是数字: %v", seat["total"])
	return int(total)
}

var billingExpiryRouter = setupRouter()

// X9：到期链路 HTTP 端到端。seed 2 个未过期 seat 单元 → overview seat.total=2；
// 把其中 1 个 expire_at 回拨到 now-1s（模拟到期，不等 30 天）→ overview seat.total=1，
// 证明 activeUnits 的 expire_at>now 读时过滤生效。
func TestBillingSeatExpiryHTTP(t *testing.T) {
	r := billingExpiryRouter

	// 唯一手机号，避免与其他测试撞用户/共享表污染。
	phone := "13911100099"
	token := registerUser(t, r, phone)

	// 从 users 表回查该用户的 user_id（注册接口用唯一手机号，回查确定）。
	var uid int64
	require.NoError(t, framework.DB.
		Raw("SELECT id FROM users WHERE phone = ?", phone).Scan(&uid).Error)
	require.NotZero(t, uid, "未查到注册用户 id")

	// 造数：给该 user 直接 INSERT 2 个 active 的 seat 单元，expire_at = now+30d。
	// 结束时按 user_id 清理自己造的行（绝不 truncate 共享表）。
	t.Cleanup(func() {
		framework.DB.Exec("DELETE FROM billing_license_units WHERE user_id = ?", uid)
		framework.DB.Exec("DELETE FROM users WHERE id = ?", uid)
	})

	now := time.Now()
	future := now.Add(30 * 24 * time.Hour)
	for i := 0; i < 2; i++ {
		require.NoError(t, framework.DB.Exec(
			`INSERT INTO billing_license_units
			 (user_id, kind, status, current_instance_id, source, source_ref, order_item_id, expire_at, created_at, updated_at)
			 VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
			uid, "seat", "active", "", "grant", "test:x9", 0, future, now, now,
		).Error)
	}

	// 初始：两个未过期 seat → seat.total = 2。
	require.Equal(t, 2, seatTotal(t, token), "seed 后应有 2 个未过期席位")

	// 回拨其中 1 个的 expire_at 到 now-1s（模拟到期，不真等时间）。status 仍 active。
	past := time.Now().Add(-1 * time.Second)
	res := framework.DB.Exec(
		`UPDATE billing_license_units SET expire_at = ?
		 WHERE id = (SELECT id FROM billing_license_units WHERE user_id = ? AND kind = ? ORDER BY id ASC LIMIT 1)`,
		past, uid, "seat",
	)
	require.NoError(t, res.Error)
	require.EqualValues(t, 1, res.RowsAffected, "应恰好回拨 1 行")

	// 再查：activeUnits 的 expire_at>now 过滤生效 → seat.total = 1。
	require.Equal(t, 1, seatTotal(t, token), "1 个到期后应只剩 1 个未过期席位")
}
```

- [ ] **Step 2: 运行验证失败** —— Run: `go test ./apptest/ -run TestBillingSeatExpiryHTTP -v`；Expected：若 `activeUnits` 未按 `expire_at > now` 读时过滤（例如改成只看 `status`），回拨后 `seat.total` 仍为 2，末条 `require.Equal(t, 1, ...)` 报 `expected: 1 actual: 2` FAIL。（当前实现已过滤，故本回归测试预期直接 PASS；若要人为观察 FAIL，可临时把断言改成期望 2 验证测试有区分度，验证后改回。）

- [ ] **Step 3: 确认已有实现满足** —— 无需改生产码。核对要点：
  - `modules/billing/internal/license_repository.go:39-42` `activeUnits` 的 WHERE 为 `user_id = ? AND kind = ? AND status = ? AND expire_at > ?`（`LicenseActive` + `now`），读时即过滤到期行。
  - `modules/billing/internal/license_service.go:25-31` `Capacity` = `len(activeUnits(userID, kind, time.Now()))`。
  - `modules/billing/internal/billing2_api.go:40,61` overview 用 `Capacity(uid, KindSeat)` 填 `Data.seat.total`。
  三者串起来即"expire_at 回拨到过去 → 下次 overview 读时该席位不计入 total"，无需版本化到期任务、无需真等 30 天。

- [ ] **Step 4: 运行验证通过** —— Run: `go test ./apptest/ -run TestBillingSeatExpiryHTTP -v`；Expected：PASS（seed 后 total=2，回拨 1 个后 total=1）。

- [ ] **Step 5: 提交** —— `git add backend/apptest/billing_expiry_test.go` + commit：`test(billing): X9 到期链路 HTTP 端到端，回拨 expire_at 证明容量读时过滤`
---

### Task 13: 访问日志分页 size 钳制（S4）—— 发现并修复

**类型**：发现并修复（`modules/accesslog/internal/api.go:34-35` 解析 `page`/`size` 后未做任何钳制，`service.go:41` 直接把 `q.Size` 传进 `repository.list` 的 `Limit(limit)`；当 `size=-1` 时 GORM 执行 `LIMIT -1`——sqlite/mysql 下等价于无上限，直接拉全表，可被越权拉全量日志或打爆内存；`page=0` 还会算出 `(0-1)*size` 的负 offset）。

**Files:**
- Modify（生产码修复）: `modules/accesslog/internal/api.go`（在 `GetAccessLogList` 内 `api.go:35` 之后、构造 `ListQuery` 之前插入 page/size 钳制）
- Test（追加）: `apptest/accesslog_test.go`（追加一个测试函数，不改动现有用例）

**Interfaces:**
- Consumes: `setupRouter() *gin.Engine`、`doJSON(r, method, path, token, body) *httptest.ResponseRecorder`、`decode(t, w) framework.Response`、`adminToken(t, r) string`；`framework.DB *gorm.DB`（原生 SQL 造数）；表 `access_logs`，列 `scope/username/method/path/status_code/latency_ms/client_ip/created_at`
- Produces: 测试函数 `TestAccessLogListSizeClamped`；被改生产函数 `accesslog.GetAccessLogList`（内部钳制逻辑，签名不变）

- [ ] **Step 1: 写失败测试** —— 在 `apptest/accesslog_test.go` 末尾追加（现有 import 已含 `net/http`/`testing`/`assert`，本用例新增需要 `fmt`、`time`、`manager-backend/framework`；若这些 import 尚未在文件顶部，请一并补入 import 块）：

```go
// TestAccessLogListSizeClamped 验证 page/size 非法值（page=0、size=-1）被钳制，
// 不会触发 LIMIT(-1) 拉全表，也不 500；返回受钳制的正常分页（默认 size=20，list 长度 ≤200）。
// S4：size 未钳制补测。
func TestAccessLogListSizeClamped(t *testing.T) {
	r := setupRouter()
	tok := adminToken(t, r)

	// 直接造 250 行日志（超过默认页 20、也超过上限 200），验证钳制生效而非拉全表。
	marker := fmt.Sprintf("/s4-clamp/%d", time.Now().UnixNano())
	now := time.Now()
	for i := 0; i < 250; i++ {
		err := framework.DB.Exec(
			`INSERT INTO access_logs (user_id, scope, username, method, path, status_code, latency_ms, client_ip, created_at) VALUES (?,?,?,?,?,?,?,?,?)`,
			0, "staff", "s4clamp", "GET", marker, 200, 1, "127.0.0.1", now,
		).Error
		if err != nil {
			t.Fatalf("seed access_logs 失败: %v", err)
		}
	}
	t.Cleanup(func() {
		framework.DB.Exec(`DELETE FROM access_logs WHERE path = ?`, marker)
	})

	// page=0、size=-1：修复前 size=-1 会传进 Limit(-1) 拉全表（250 行），或产生负 offset。
	w := doJSON(r, "GET", "/api/v1/access-log/list?page=0&size=-1", tok, nil)
	assert.Equal(t, http.StatusOK, w.Code, "非法分页参数不应 500")

	resp := decode(t, w)
	assert.Equal(t, 0, resp.Code)

	data, ok := resp.Data.(map[string]interface{})
	if !assert.True(t, ok, "Data 应为分页对象") {
		return
	}
	list, ok := data["list"].([]interface{})
	if !assert.True(t, ok, "list 应为数组") {
		return
	}
	// 钳制后应回落默认页大小（20），无论如何都不得超过上限 200（更不能是全表 250）。
	assert.LessOrEqual(t, len(list), 200, "size 未钳制则会拉全表")
	assert.Equal(t, 20, len(list), "size=-1 应回落默认 20")

	// total 反映过滤后的真实总数（≥250），确认造数生效、count 正常。
	total, ok := data["total"].(float64)
	assert.True(t, ok, "total 应为数字")
	assert.GreaterOrEqual(t, total, float64(250))
}
```

- [ ] **Step 2: 运行验证失败** —— Run: `go test ./apptest/ -run TestAccessLogListSizeClamped -v`；Expected: FAIL。当前 `api.go` 不钳制，`size=-1` 经 `service.GetList` 传入 `repo.list` 的 `Limit(-1)`（sqlite 下 `LIMIT -1` = 无限制）拉回全部 250 行，断言 `len(list)==20`（及 `<=200`）失败（实际 250）。

- [ ] **Step 3: 实现/修复** —— 编辑 `modules/accesslog/internal/api.go` 的 `GetAccessLogList`，在解析完 `page`/`size`（原第 34-35 行）之后、构造 `ListQuery` 之前插入钳制，沿用本仓惯例（见 `openapi/internal/open_api.go:134-139`、`billing/internal/service.go:43-48`：`page<1→1`、`size<1||size>200→20`）。

改动后该函数开头形如：

```go
func GetAccessLogList(c *gin.Context) {
	page, _ := strconv.Atoi(c.DefaultQuery("page", "1"))
	size, _ := strconv.Atoi(c.DefaultQuery("size", "20"))
	// 钳制分页参数，避免 size=-1 触发 Limit(-1) 拉全表、page=0 产生负 offset（S4）。
	if page < 1 {
		page = 1
	}
	if size < 1 || size > 200 {
		size = 20
	}

	list, total, err := AccessLogService.GetList(ListQuery{
		Page:        page,
		Size:        size,
		Username:    c.Query("username"),
		Scope:       c.Query("scope"),
		Method:      c.Query("method"),
		Path:        c.Query("path"),
		StatusGroup: c.Query("status_group"),
		StartTime:   c.Query("start_time"),
		EndTime:     c.Query("end_time"),
		Order:       c.Query("order"),
		Sort:        c.Query("sort"),
	})
	if err != nil {
		framework.FailErr(c, err)
		return
	}
	framework.OKWithPage(c, list, total)
}
```

具体以 Edit 精确替换原第 34-35 两行为「解析 + 两个钳制 if 块」即可（仅在这两行后新增 6 行钳制逻辑，其余不动）。

- [ ] **Step 4: 运行验证通过** —— Run: `go test ./apptest/ -run TestAccessLogListSizeClamped -v` 然后 `go build ./... && go vet ./modules/accesslog/... ./apptest/...`；Expected: PASS（`size` 回落 20，`len(list)==20`，HTTP 200，`total>=250`）。

- [ ] **Step 5: 提交** —— 
```bash
git add backend/modules/accesslog/internal/api.go backend/apptest/accesslog_test.go
git commit -m "fix(accesslog): 列表分页 size 钳制防拉全表(S4)

page<1 归 1，size<1 或 >200 归默认 20，避免 size=-1 触发 Limit(-1) 拉全表、
page=0 产生负 offset；追加 apptest 覆盖非法分页参数。

发现记录：S4 补测暴露、已修"
```
---

## Self-Review（本计划对照总纲自查结论）

**1. Spec 覆盖**：总纲 §5 安全回归矩阵与 §6 计费时间测法逐条对照——
- 严重(5)：S1/A1/A2→Task 2，B1→Task 7，F2→Task 1 审计（已有 `config_secrets_test.go`）。✓
- 高(9)：A3→Task 2，A4→Task 3，B2→Task 7，B3→Task 8，I1→Task 4，O3/C1/F1→Task 1 审计（各有单测），S2→Task 6。✓
- 入选中(A5→Task 3、I2→Task 5、B5→Task 9、S4→Task 13、S3/席位回收→Task 1 审计引用)。✓
- §6 计费时间：到期读时过滤/markExpired→Task 10，续费叠加/永久单元→Task 11，到期 HTTP E2E→Task 12，试用到期→Task 9/10。✓
- 明确不在 Plan A（归 Plan D，非静默省略）：C6 差集回报、O6 fullKey 脱敏、B4 Settle 并发、B6 充值上限。

**2. 占位扫描**：全文无 TODO/TBD/待补充/"类似上文"；每个"写代码"步骤均含完整可编译 Go；唯一的 `framework.DB()` 字样是全局约束里"勿写 `framework.DB()`"的告诫，非代码。

**3. 类型一致性**：已消除跨任务同包重名——`staffIDByUsername`/`staffIsSuperuser`/`createRole`/`assignRoles` 仅 Task 2 定义（Task 3 复用）；`cleanupLicenseUnitsByUser(t,uid int)` 仅 Task 10 定义（Task 11 复用）；`framework.DB` 统一为包变量用法。`LicenseUnit.UserID uint` 与 `activeUnits(userID int)` 的转换已在相关任务标注。

**4. 执行者须知（残余风险）**：
- 各任务代码中的路由/字段/权限标识均据真实文件核对，但仍以 TDD 为准——Step 2 若因签名细节编译失败，按 Step 说明对照引用文件（已给 file:line）修正后再跑，不改变断言语义。
- Task 5（I2）、Task 13（S4）是**改生产码**的发现-修复任务；其 Step 2 预期 **FAIL**（暴露缺陷），Step 3 修复后 Step 4 转 PASS，并在 Task 1 审计文档补记。其余为回归防护任务，正确实现下 Step 2 即应 PASS。
- 全部完成后跑一次全量：`cd backend && gofmt -l . && go vet ./... && go test ./... -count=1 && go test ./framework/ -run TestModuleBoundaries` 应全绿。
